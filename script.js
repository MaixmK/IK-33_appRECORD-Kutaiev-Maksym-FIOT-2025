(function () {
  const tabs = Array.from(document.querySelectorAll('.top-nav .tab'));
  const sideMenuLab1 = document.getElementById('sideMenuLab1');
  const sideMenuLab2 = document.getElementById('sideMenuLab2');
  const sideMenuLab3 = document.getElementById('sideMenuLab3');
  const sideMenuLab4 = document.getElementById('sideMenuLab4');
  const sideMenuLab5 = document.getElementById('sideMenuLab5');
  const sideMenuLab6 = document.getElementById('sideMenuLab6');
  const sideMenuLab7 = document.getElementById('sideMenuLab7');
  const sideMenuLab8 = document.getElementById('sideMenuLab8');

  const content = document.getElementById('content');

  const codeBlock = (s) =>
    `<pre class="code">${String(s)
      .replaceAll('&', '&amp;')
      .replaceAll('<', '&lt;')
      .replaceAll('>', '&gt;')}</pre>`;

  const lab1 = {
    // Тема/мета/розташування
theme: `
  <h2>Практична робота №1 — Тема, мета, розташування</h2>
  <p><b>Тема практичної роботи:</b> ТЕГИ ТА АТРИБУТИ HTML-ДОКУМЕНТА. СТРУКТУРНА РОЗМІТКА. GIT.GITHUB. РОБОТА З РЕПОЗИТОРІЯМИ. ОПИС ЛОГІКИ ВЛАСНОГО WEB-ЗАСТОСУНКУ.</p>
  <p><b>Мета практичної роботи:</b> придбати практичні навички роботи з GitHub, репозиторіями, HTML-документом, таблицями, зображеннями, посиланнями, списками, формами. Створити шаблон звітного HTML-документа для відображення результатів роботи всіх лабораторних робіт.</p>
  <p><b>Тема розробки:</b> веб-застосунок <i>«Конфігуратор ПК»</i> для підбору комплектуючих, перевірки сумісності та формування готових збірок.</p>
  <p><b>Мета розробки:</b> створити сучасний веб-застосунок, який надає зручні засоби пошуку, фільтрації, перевірки сумісності компонентів, збереження та оформлення збірок ПК, а також особистий кабінет із історією.</p>
  <ul>
    <li>дослідження предметної області ринку ПК-компонентів та наявних конфігураторів;</li>
    <li>формування вимог до інформаційної системи для роботи зі збірками та сумісністю;</li>
    <li>розробка інтерфейсу з інтуїтивною взаємодією (пошук, фільтри, кроковий підбір сумісних деталей);</li>
    <li>перевірка працездатності механізмів пошуку/підбору та коректності бізнес-логіки;</li>
    <li>базові вимоги безпеки (валідація даних, коректна робота форм).</li>
  </ul>

  <h3 class="muted">Розташування</h3>

  <div class="link-buttons">
    <a class="btn" href="https://github.com/MaixmK/IK-33_appWEB-Kutaiev-Maksym-FIOT-2025" target="_blank" rel="noopener">Репозиторій застосунку</a>
    <a class="btn" href="https://maixmk.github.io/IK-33_appWEB-Kutaiev-Maksym-FIOT-2025/" target="_blank" rel="noopener">Жива сторінка застосунку</a>
    <a class="btn" href="https://github.com/MaixmK/IK-33_appRECORD-Kutaiev-Maksym-FIOT-2025" target="_blank" rel="noopener">Репозиторій звітів</a>
    <a class="btn" href="https://maixmk.github.io/IK-33_appRECORD-Kutaiev-Maksym-FIOT-2025/" target="_blank" rel="noopener">Жива сторінка звітв</a>
    <a class="btn" href="https://github.com/MaixmK/IK-33_INDEPENDENT-Kutaiev-Maksym-FIOT-2025" target="_blank" rel="noopener">Репозиторій із самостійною роботою</a>
    <a class="btn" href="https://maixmk.github.io/IK-33_INDEPENDENT-Kutaiev-Maksym-FIOT-2025/" target="_blank" rel="noopener">Жива сторінка самостійної роботи</a>
    <a class="btn" href="https://www.figma.com/design/S3tM6vTLjBuZINItOYO71u/Untitled?node-id=0-1&t=nIZJZHqGCv9BfEZN-1" target="_blank" rel="noopener">Figma макет</a>
  </div>
`,



    // Опис предметного середовища
    env: `
      <h2>1. Опис предметного середовища (функціонал майбутнього WEB-застосунку)</h2>
      <p>WEB-застосунок допомагає користувачам підібрати комплектуючі для власного комп’ютера або вибрати готову збірку.</p>

      <h3>Функціонал:</h3>
      <ul>
        <li>Каталог комплектуючих (процесори, відеокарти, материнські плати, пам’ять, блоки живлення тощо).</li>
        <li>Фільтри (сумісність, ціна, бренд, продуктивність).</li>
        <li>Інтерактивний конструктор: користувач обирає процесор → система пропонує сумісну материнську плату і т.д.</li>
        <li>Перевірка сумісності компонентів.</li>
        <li>Кошик з можливістю замовлення всієї збірки.</li>
        <li>Особистий кабінет з історією збірок.</li>
        <li>Адмін-панель (додавання комплектуючих, редагування інформації).</li>
      </ul>

      <h2>2. Бізнес-логіка</h2>
      <p><strong>Скорочений опис:</strong> Користувач створює власну збірку → обирає деталі → система перевіряє сумісність → користувач може зберегти збірку, замовити її або поділитися посиланням.</p>

      <h3>Сценарій:</h3>
      <ol>
        <li>Користувач заходить на сайт.</li>
        <li>Обирає режим: «Конструктор» або «Готові збірки».</li>
        <li>У конструкторі додає комплектуючі (CPU → GPU → RAM тощо).</li>
        <li>Система перевіряє сумісність (наприклад, чи підходить сокет процесора до материнської плати).</li>
        <li>Користувач зберігає збірку у профілі або додає все у кошик.</li>
        <li>Користувач оформлює замовлення.</li>
        <li>Адміністратор підтверджує і відправляє комплект.</li>
      </ol>

      <h2>3. Функціональні вимоги</h2>
      <ul>
        <li>Каталог комплектуючих з фільтрами.</li>
        <li>Інтерактивний конструктор ПК.</li>
        <li>Перевірка сумісності обраних компонентів.</li>
        <li>Можливість додавання збірки у кошик.</li>
        <li>Особистий кабінет з історією збірок.</li>
        <li>Адмін-панель для управління товарами.</li>
        <li>Система відгуків для комплектуючих.</li>
      </ul>

      <h2>4. Нефункціональні вимоги</h2>
      <ul>
        <li>Адаптивність: сайт зручний на ПК, планшеті, смартфоні.</li>
        <li>Безпека: дані користувачів зберігаються у захищеному вигляді.</li>
        <li>Масштабованість: можливість додавання нових категорій комплектуючих.</li>
        <li>Юзабіліті: простий і зрозумілий інтерфейс навіть для користувача без технічних знань.</li>
      </ul>
    `,

    // Структура головної
    structure: `
      <h2>Структура головної сторінки (<code>index.html</code>)</h2>
      <ul>
        <li><b>Шапка:</b> заголовок «Конфігуратор ПК» + навігація (Головна, Про проєкт).</li>
        <li><b>Секції:</b> пошук комплектуючих (форма), перелік компонентів (іконки + підписи), таблиця сумісності, форма замовлення, популярні збірки (список).</li>
        <li><b>Посилання:</b> внутрішні переходи між сторінками.</li>
      </ul>

      ${codeBlock(`<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Конфігуратор ПК</title>
</head>
<body>
  <header>
    <h1>Конфігуратор ПК</h1>
    <nav>
      <ul>
        <li><a href="index.html" aria-current="page">Головна</a></li>
        <li><a href="about.html">Про проєкт</a></li>
      </ul>
    </nav>
  </header>

  <main>
    <section aria-labelledby="search-heading">
      <h2>Пошук комплектуючих</h2>
      <form action="#" method="get">
        <label for="search">Введіть назву:</label>
        <input id="search" type="text" name="search" placeholder="Я шукаю...">
        <button type="submit">Знайти</button>
      </form>
    </section>

    <section aria-labelledby="components-heading">
      <h2>Компоненти збірки</h2>
      <ul>
        <li><img src="img/cpu.svg" alt="Процесор" width="63" height="63"> Процесор</li>
        <li><img src="img/Motherboard.svg" alt="Материнська плата" width="63" height="63"> Материнська плата</li>
        <li><img src="img/RAM.svg" alt="Оперативна пам'ять" width="63" height="63"> Оперативна пам'ять</li>
        <li><img src="img/gpu.svg" alt="Відеокарта" width="63" height="63"> Відеокарта</li>
        <li><img src="img/harddrive.svg" alt="Накопичувач" width="63" height="63"> Накопичувач</li>
        <li><img src="img/comp.svg" alt="Корпус" width="63" height="63"> Корпус</li>
        <li><img src="img/power-supply.png" alt="Блок живлення" width="63" height="63"> Блок живлення</li>
      </ul>
    </section>

    <section aria-labelledby="table-heading">
      <h2>Таблиця сумісності</h2>
      <table border="1">
        <caption>Перевірка сумісності компонентів</caption>
          <tr>
            <th>Компонент</th>
            <th>Сумісність</th>
          </tr>
          <tr>
            <td>Процесор Intel i5</td>
            <td>Сумісний з Socket 1200</td>
          </tr>
          <tr>
            <td>Відеокарта RTX 4060</td>
            <td>Потрібен блок живлення 600W+</td>
          </tr>
      </table>
    </section>

    <section aria-labelledby="order-heading">
      <h2>Оформлення замовлення</h2>
      <form action="#" method="post">
          <label for="name">Ім’я:</label>
          <input id="name" type="text" name="name" required><br>

          <label for="phone">Телефон:</label>
          <input id="phone" type="tel" name="phone" required><br>

          <label for="address">Адреса:</label>
          <input id="address" type="text" name="address"><br>


          <label for="delivery">Спосіб доставки:</label>
          <select id="delivery" name="delivery">
            <option value="nova-poshta">Нова Пошта</option>
            <option value="ukrposhta">Укрпошта</option>
            <option value="courier">Кур’єр</option>
          </select>


        <button type="submit">Підтвердити замовлення</button>
      </form>
    </section>

    <section aria-labelledby="popular-heading">
      <h2>Популярні збірки</h2>
      <ol>
        <li>Ігрова збірка (RTX 4060, Ryzen 5, 16GB RAM)</li>
        <li>Офісний ПК (Intel i3, 8GB RAM, SSD 512GB)</li>
        <li>Універсальний ПК (Ryzen 7, 32GB RAM, RTX 4070)</li>
      </ol>
    </section>
  </main>

  <footer>
    <p>&copy; 2025 Конфігуратор ПК. Всі права захищені.</p>
  </footer>
</body>
</html>
      `)}
    `,

    // Таблиця
    "table-code": `

      <h3>Код (з <code>index.html</code>)</h3>
      ${codeBlock(`<table border="1">
        <caption>Перевірка сумісності компонентів</caption>
          <tr>
            <th>Компонент</th>
            <th>Сумісність</th>
          </tr>
          <tr>
            <td>Процесор Intel i5</td>
            <td>Сумісний з Socket 1200</td>
          </tr>
          <tr>
            <td>Відеокарта RTX 4060</td>
            <td>Потрібен блок живлення 600W+</td>
          </tr>
      </table>`)}
      <h3>Скрін</h3>
      <img src="laba1/img/table.png" alt="Скріншот таблиці"/>

      <h3>Теорія</h3>
      <ul>
        <li>Теги: <code>&lt;table&gt;</code>, <code>&lt;tr&gt;</code>, <code>&lt;th&gt;</code>, <code>&lt;td&gt;</code>, <code>&lt;caption&gt;</code>.</li>
        <li>Атрибути: <code>colspan</code>, <code>rowspan</code> (об’єднання комірок).</li>
        <li>Оформлення краще робити через CSS: рамки, <code>border-collapse</code>, відступи.</li>
      </ul>
    `,

    // Зображення
    "image-code": `

      <h3>Код (з <code>index.html</code>)</h3>
      ${codeBlock(`<img src="img/cpu.svg" alt="Процесор" width="63" height="63">`)}
      <h3>Скрін</h3>
      <img src="laba1/img/cpulab1.png" alt="Скрін зображення"/>

      <h3>Теорія</h3>
      <ul>
        <li><code>&lt;img&gt;</code> — вставка зображення; альтернативний текст у <code>alt</code>.</li>
        <li>Розміри через <code>width</code>/<code>height</code> або CSS</li>
      </ul>
    `,

    // Форма
    "form-code": `

      ${codeBlock(`<form action="#" method="post">
  <label for="name">Ім’я:</label>
  <input id="name" type="text" name="name" required><br>

  <label for="phone">Телефон:</label>
  <input id="phone" type="tel" name="phone" required><br>

  <label for="address">Адреса:</label>
  <input id="address" type="text" name="address"><br>

  <label for="delivery">Спосіб доставки:</label>
  <select id="delivery" name="delivery">
    <option value="nova-poshta">Нова Пошта</option>
    <option value="ukrposhta">Укрпошта</option>
    <option value="courier">Кур’єр</option>
  </select>

  <button type="submit">Підтвердити замовлення</button>
</form>`)}
      <p>Ключові атрибути форми: <code>action</code>, <code>method</code>; для полів — <code>name</code>, <code>type</code>, <code>required</code>, <code>placeholder</code>.</p>
    `,

    // Списки
    "html-li": `
      <h2>HTML-код нумерованого списку (фрагмент головної навігації)</h2>
      ${codeBlock(`<header>
  <h1>Конфігуратор ПК</h1>
  <nav>
    <ul>
      <li><a href="index.html" aria-current="page">Головна</a></li>
      <li><a href="about.html">Про проєкт</a></li>
    </ul>
  </nav>
</header>`)}
        <p>Скрін</p>
        <img src="laba1/img/ul.png" alt="Скріншот шапки"/>
        <h2>HTML-код нумерованого списку (популярні збірки)</h2>
      ${codeBlock(`<section>
  <h2>Популярні збірки</h2>
  <ol>
        <li>Ігрова збірка (RTX 4060, Ryzen 5, 16GB RAM)</li>
        <li>Офісний ПК (Intel i3, 8GB RAM, SSD 512GB)</li>
        <li>Універсальний ПК (Ryzen 7, 32GB RAM, RTX 4070)</li>
      </ol>
    </section>`)}
        <p>Скрін</p>
        <img src="laba1/img/ol.png" alt="Скріншот нумерованого списку"/>

          <h3>Коротка теорія</h3>
  <ul>
    <li><b>&lt;ul&gt;</b> — маркований список, <b>&lt;ol&gt;</b> — нумерований, елемент — <b>&lt;li&gt;</b>.</li>
    <li><b>&lt;dl&gt;</b> — описовий список: термін <b>&lt;dt&gt;</b> і опис <b>&lt;dd&gt;</b>.</li>
    <li>Атрибути для <b>&lt;ol&gt;</b>: <code>start</code> (початок нумерації), <code>reversed</code> (зворотний порядок), <code>type</code> (1, a, A, i, I).</li>
    <li>У <b>&lt;li&gt;</b> можна задати <code>value</code> для конкретного номера в <b>&lt;ol&gt;</b>.</li>
    <li>Стиль маркерів задавай через CSS: <code>list-style-type</code>, відступи — <code>margin/padding</code>.</li>
    <li>Вкладайте списки всередину <b>&lt;li&gt;</b> для підрівнів; для меню доречно <code>&lt;nav&gt;&lt;ul&gt;...&lt;/ul&gt;&lt;/nav&gt;</code>.</li>
  </ul>
    `,

    // Висновки
conclusion: `
  <h2>Висновки</h2>
  <ul>
    <li>Початково створено макет інтерфейсу у Figma, на основі якого спроєктовано структуру сторінок.</li>
    <li>Реалізовано семантичну розмітку головної: шапка й навігація, пошук, перелік компонентів з іконками, таблиця сумісності, форма замовлення, «Популярні збірки», футер.</li>
    <li>Додано приклади та <b>теорію з HTML-кодом і скріншотами</b> для таблиць, зображень і списків; враховано корисні атрибути та аспекти доступності.</li>
    <li>Звіт виконано як SPA: перемикання розділів без перезавантаження.</li>
    <li>Запроваджено хелпер <code>codeBlock()</code> для безпечного показу великих HTML-фрагментів без конфліктів з DOM.</li>
    <li>Налаштовано репозиторій Git/GitHub (структура проєкту, коміти, публікація), підготовлено звітний документ.</li>
  </ul>
`,


  };

  const lab2 = {

    theme: `
  <h2>Практична робота №2 — Тема, мета, розташування</h2>

  <p><b>Тема практичної роботи:</b> КАСКАДНІ ТАБЛИЦІ СТИЛІВ (CSS). СЕЛЕКТОРИ ТЕГУ, КЛАСУ, ІДЕНТИФІКАТОРА. ІНШІ СЕЛЕКТОРИ. СТИЛЬОВЕ ОФОРМЛЕННЯ ТЕКСТОВИХ ЕЛЕМЕНТІВ, СПИСКІВ, ТАБЛИЦЬ.</p>

  <p><b>Мета практичної роботи:</b> придбати практичні навички роботи з селекторами CSS різних типів (тегу, класу, ідентифікатора, атрибута, дочірні, сусідні), навчитися створювати та застосовувати каскадні стилі для оформлення елементів веб-сторінки, таблиць, списків, текстових блоків, а також формувати грамотну структуру стилів у проєкті.</p>

  <p><b>Тема розробки:</b> веб-застосунок <i>«Конфігуратор ПК»</i>, який включає сторінки каталогу компонентів, головну сторінку з таблицями та списками, інтерактивні елементи інтерфейсу та оформлення резюме згідно макету.</p>

  <p><b>Мета розробки:</b> покращити зовнішній вигляд і користувацький досвід веб-застосунку через коректне використання каскадних таблиць стилів та типових UI-патернів.</p>

  <ul>
    <li>закріпити знання основних типів селекторів (тегу, класу, id) та їхнього пріоритету;</li>
    <li>застосувати універсальні, дочірні, нащадкові, сусідні та селектори атрибута у реальних макетах;</li>
    <li>разробити оформлення текстових елементів, блоків, списків, панелей та таблиць;</li>
    <li>виконати повне стильове оформлення сторінки резюме за власним макетом;</li>
    <li>оформити структурований звіт у HTML-документі з прикладами CSS-коду.</li>
  </ul>

  <h3 class="muted">Розташування</h3>

  <div class="link-buttons">
    <a class="btn" href="https://github.com/MaixmK/IK-33_appWEB-Kutaiev-Maksym-FIOT-2025" target="_blank" rel="noopener">Репозиторій застосунку</a>
    <a class="btn" href="https://maixmk.github.io/IK-33_appWEB-Kutaiev-Maksym-FIOT-2025/" target="_blank" rel="noopener">Жива сторінка застосунку</a>
    <a class="btn" href="https://github.com/MaixmK/IK-33_appRECORD-Kutaiev-Maksym-FIOT-2025" target="_blank" rel="noopener">Репозиторій звітів</a>
    <a class="btn" href="https://maixmk.github.io/IK-33_appRECORD-Kutaiev-Maksym-FIOT-2025/" target="_blank" rel="noopener">Жива сторінка звітів</a>
    <a class="btn" href="https://github.com/MaixmK/IK-33_INDEPENDENT-Kutaiev-Maksym-FIOT-2025" target="_blank" rel="noopener">Репозиторій із самостійною роботою</a>
    <a class="btn" href="https://maixmk.github.io/IK-33_INDEPENDENT-Kutaiev-Maksym-FIOT-2025/" target="_blank" rel="noopener">Жива сторінка самостійної</a>
    <a class="btn" href="https://www.figma.com/design/S3tM6vTLjBuZINItOYO71u/Untitled?node-id=0-1" target="_blank" rel="noopener">Figma макет</a>
    <a class="btn" href="https://maixmk.github.io/resume/" target="_blank" rel="noopener">Резюме</a>
  </div>
  </div>
`,

    "theory":`
    <h2>Способи підключення стилів</h2>

<p>
  У CSS існує три основні способи підключення таблиць стилів до HTML-документа.
  Кожен із них має свої переваги та використовується в різних ситуаціях під час розробки веб-застосунків.
  Нижче наведено порівняння та приклади кожного способу.
</p>

<h3>1. Вбудовані (inline) стилі</h3>
<p>
  CSS записується безпосередньо всередині HTML-тега в атрибуті <code>style</code>.
  Такий спосіб застосовується для швидкого одноразового форматування.
</p>

${codeBlock(`<p style="color: green; font-size: 20px;">
  Привіт! Це текст із вбудованим стилем.
</p>`)}

<p><b>Переваги:</b></p>
<ul>
  <li>Дуже проста реалізація.</li>
  <li>Корисно для одноразових експериментів.</li>
</ul>

<p><b>Недоліки:</b></p>
<ul>
  <li>Порушує принцип розділення структури і стилів.</li>
  <li>Незручно підтримувати в масштабних проєктах.</li>
</ul>

<hr>

<h3>2. Внутрішні стилі (internal CSS)</h3>
<p>
  Стилі записуються у блоці <code>&lt;style&gt;</code>, який розміщується всередині
  секції <code>&lt;head&gt;</code>. Такий спосіб зручний, коли проєкт складається з одного HTML-файла.
</p>

${codeBlock(`<head>
  <style>
    h1 {
      color: #2ea44f;
      text-align: center;
    }
  </style>
</head>`)}

<p><b>Переваги:</b></p>
<ul>
  <li>Стилі зберігаються в одному місці.</li>
  <li>Не потрібні додаткові файли.</li>
</ul>

<p><b>Недоліки:</b></p>
<ul>
  <li>Незручно для великих проєктів — HTML стає надто “важким”.</li>
  <li>Не можна повторно використовувати стилі в інших сторінках.</li>
</ul>

<hr>

<h3>3. Зовнішні таблиці стилів (external CSS)</h3>
<p>
  Стилі зберігаються в окремому файлі <code>.css</code>, а підключаються
  через тег <code>&lt;link&gt;</code> у <code>&lt;head&gt;</code>.
  Це найпоширеніший і найправильніший спосіб у сучасній розробці.
</p>

${codeBlock(`<head>
  <link rel="stylesheet" href="styles.css">
</head>`)}

<p><b>Переваги:</b></p>
<ul>
  <li>Найкраща структура проєкту.</li>
  <li>Вміст HTML залишається чистим і легким.</li>
  <li>Можна використовувати один і той самий CSS-файл для багатьох сторінок.</li>
  <li>Підтримка кешування браузером → сайт працює швидше.</li>
</ul>

<p><b>Недоліки:</b></p>
<ul>
  <li>Потрібен додатковий файл (хоча це не критично).</li>
</ul>

<hr>

<h3>4. Імпорт стилів у CSS (режим @import)</h3>
<p>
  Дозволяє підключати один CSS-файл усередині іншого.
  Синтаксис використовує директиву <code>@import</code>.
</p>

${codeBlock(`/* main.css */
@import url("reset.css");
@import url("variables.css");`)}

<p><b>Переваги:</b></p>
<ul>
  <li>Зручно розбивати стилі на модулі.</li>
</ul>

<p><b>Недоліки:</b></p>
<ul>
  <li>Завантажується повільніше, ніж <code>&lt;link&gt;</code>.</li>
  <li>У сучасних проєктах використовується рідко через продуктивність.</li>
</ul>

<hr>

<h3>5. Порівняльна таблиця способів підключення</h3>

<table class="component-table">
  <thead>
    <tr>
      <th>Спосіб</th>
      <th>Сфера використання</th>
      <th>Переваги</th>
      <th>Недоліки</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Inline стилі</td>
      <td>Разові зміни, тестування</td>
      <td>Швидко, просто</td>
      <td>Погана підтримка, засмічує HTML</td>
    </tr>
    <tr>
      <td>Internal CSS</td>
      <td>Малі проєкти, одна сторінка</td>
      <td>Усі стилі в одному файлі</td>
      <td>Немає повторного використання</td>
    </tr>
    <tr>
      <td>External CSS</td>
      <td>Усі сучасні сайти</td>
      <td>Кешування, чиста структура, багаторазове використання</td>
      <td>Потрібно додавати файл</td>
    </tr>
    <tr>
      <td>@import</td>
      <td>Модульні CSS-архітектури</td>
      <td>Зручно групувати стилі</td>
      <td>Повільніша загрузка</td>
    </tr>
  </tbody>
</table>
`,
    // 1. Селектори тегу
    "sel-tags": `
      <h2>1. Селектори тегу</h2>
      <p>Селектори тегу застосовуються одразу до всіх елементів певного типу. У застосунку я використовую їх для базової типографіки та роботи зі списками.</p>

      <h3>Приклад 1 — заголовки сторінок</h3>
      ${codeBlock(`h1, h2, h3 {
  font-weight: 700;
  color: #111;
  margin: 0 0 10px;
}`)}

      <p><b>HTML-фрагмент:</b></p>
      ${codeBlock(`<h1 class="hero__title">Збери свій ідеальний ПК</h1>
<h2 class="section-title">Most popular products</h2>`)}

      <figure>
        <img src="laba2/img/h1.png" alt="Скрін заголовків h1/h2">
        <img src="laba2/img/h1(1).png" alt="Скрін заголовків h1/h2">
        <figcaption>Заголовки, до яких застосовано селектор тегу <code>h1, h2</code></figcaption>
      </figure>
    `,

    // 2. Селектори класів
    "sel-classes": `
      <h2>2. Селектори класів</h2>
      <p>Селектори класів використовуються для оформлення окремих блоків інтерфейсу: шапки, пошуку, карток, таблиць тощо.</p>

      <h3>Приклад — верхня панель</h3>
      ${codeBlock(`/* HEADER */
.site-header { background: #111; color: #fff; position: sticky; top: 0; z-index: 100; }
.topbar { display: flex; align-items: center; gap: .75rem; padding: .6rem 0; }
.logo { color: #bdbdbd; text-decoration: none; font-weight: 600; font-size: 1.2rem; }
.actions { margin-left: auto; display: flex; gap: .5rem; }
.icon-btn { display: grid; place-items: center; width: 2rem; aspect-ratio: 1/1; border-radius: 50%; background: #2a2a2a; color: #fff; }
.icon-btn:hover { background: #3a3a3a; }

.burger { width: 2rem; aspect-ratio: 1/1; background: transparent; border: 0; display: grid; place-items: center; cursor: pointer; }
.burger span { width: 22px; height: 2px; background: #fff; margin: 3px 0; transition: .2s; }

.nav__list { list-style: none; display: flex; gap: 1rem; margin: 0; padding: 0; }
.nav > .nav__list > li > a { color: #ccc; text-decoration: none; font-weight: 500; }
.nav__link:hover { color: #fff; }`)}

      <p><b>HTML-фрагмент:</b></p>
      ${codeBlock(`<header class="site-header">
    <div class="topbar container">
      <a class="logo" href="#">Комплектуючі</a>
      </form>
      <nav class="nav">
        <ul class="nav__list">
          <li><a href="#shop" class="nav__link">Каталог</a></li>
          <li><a href="#about" class="nav__link">Про нас</a></li>
          <li><a href="#contacts" class="nav__link">Контакти</a></li>
        </ul>
      </nav>
      <div class="actions">
        <a class="icon-btn" href="login.html" title="Профіль">👤</a>
        <a class="icon-btn" href="#" title="Кошик">🛒</a><span id="cartCount">0</span>
      </div>
    </div>
  </header>
`)}

      <figure>
        <img src="laba2/img/topbar.png" width = 800 height = auto alt="Скрін шапки сайту">
        <figcaption>Шапка сайту з формою пошуку, оформлена селекторами класів</figcaption>
      </figure>

      <h3>Приклад — картка на головній сторінці</h3>
      ${codeBlock(`.card {
  flex: 1 1 calc(25% - 1rem);
  transition: transform .2s ease, box-shadow .2s ease;
}
.card__link {
  display: block;
  background: #fff;
  border-radius: var(--radius);
  padding: 1.2rem;
  text-decoration: none;
  color: inherit;
  text-align: center;
  box-shadow: var(--shadow);
}
.card__link:hover { transform: translateY(-5px); }
.card__icon { width: 80px; height: 80px; }
.card__title { font-weight: 600; }`)}

      <p><b>HTML-фрагмент:</b></p>
      ${codeBlock(`<li class="card" data-cat="cpu">
  <a href="#" class="card__link">
    <img src="img/cpu.svg" class="card__icon" alt="CPU">
    <div class="card__title">Процесори</div>
  </a>
</li>
`)}

      <figure>
        <img src="laba2/img/classcard.png" alt="картка категорії">
        <figcaption>Картка категорії</figcaption>
      </figure>
    `,

    // 3. Селектори ідентифікаторів
    "sel-id": `
      <h2>3. Селектори ідентифікаторів</h2>
      <p>Селектори ідентифікаторів використовуються для стилізації унікальних елементів сторінки. У моєму застосунку так оформлено поле пошуку.</p>

      <h3>CSS</h3>
      ${codeBlock(`#about ul {
  list-style: none;
  margin: 1rem 0 0;
  padding: 0;
}


#about li {
  position: relative;
  padding-left: 1.5rem;
  margin-bottom: 0.6rem;
  font-size: 1.05rem;
  color: #333;
}

#about li::before {
  content: "✔";
  position: absolute;
  left: 0;
  top: 0;
  color: #2fb06b; 
  font-size: 0.95rem;
  line-height: 1.2;
}`)}

      <h3>HTML-фрагмент</h3>
      ${codeBlock(`    <section id="about" class="home-section container">
      <h2>Чому варто обирати нас</h2>
      <ul>
        <li>Перевірена сумісність комплектуючих.</li>
        <li>Швидка доставка по Україні.</li>
        <li>Технічна підтримка 24/7.</li>
      </ul>
    </section>`)}

      <figure>
        <img src="laba2/img/li.png" alt="Скрін ul/li">
        <figcaption>Список компонентів, стилізований через селектори тегу <code>ul</code> і <code>li</code></figcaption>
      </figure>
    `,

// 4. Інші селектори
"sel-other": `
  <h2>4. Інші селектори</h2>
  <p>
    Згідно з пунктом 4 завдання, у моєму WEB-застосунку використано
    універсальний селектор, селектор атрибута, дочірні та сусідні селектори,
    а також псевдокласи і псевдоелементи. Нижче наведено реальні приклади з проєкту.
  </p>

  <h3>4.1. Універсальний селектор</h3>
  <p>Універсальний селектор <code>*</code> застосовано для задання єдиної моделі розмірів елементів:</p>
  ${codeBlock(`* { 
  box-sizing: border-box; 
}`)}
  <p>
    Такий підхід спрощує верстку: усі елементи враховують бордери та падінги
    у своїй ширині/висоті, що дозволяє уникати «ламаючих» розрахунків.
  </p>

  <h3>4.2. Селектор атрибута</h3>
  <p>
    Селектори атрибута використані для зміни стилю іконок карток каталогу
    залежно від значення атрибута <code>data-cat</code>:
  </p>
  ${codeBlock(`.card[data-cat="cpu"] .card__icon { background: #fff1e0; }
.card[data-cat="mb"] .card__icon { background: #e5f1ff; }
.card[data-cat="ram"] .card__icon { background: #f0fff0; }
.card[data-cat="gpu"] .card__icon { background: #ececff; }`)}
  <p><b>HTML-фрагмент:</b></p>
  ${codeBlock(`<ul class="catalog">
  <li class="card" data-cat="cpu">
    <a href="#" class="card__link">
      <img src="img/cpu.svg" class="card__icon" alt="CPU">
      <div class="card__title">Процесори</div>
    </a>
  </li>
  <!-- інші картки з data-cat="mb", "ram", "gpu" тощо -->
</ul>`)}
  <p>
    Завдяки селектору атрибута можна змінювати фон іконок для різних категорій
    без створення окремих додаткових класів.
  </p>

  <h3>4.3. Дочірні та нащадкові селектори</h3>
  <p>Дочірні селектори використовуються в навігаційному меню в шапці сайту:</p>
  ${codeBlock(`.nav__list {
  list-style: none;
  display: flex;
  gap: 1rem;
  margin: 0;
  padding: 0;
}

/* дочірні елементи меню всередині .nav */
.nav > .nav__list > li > a {
  color: #ccc;
  text-decoration: none;
  font-weight: 500;
}

.nav__link:hover {
  color: #fff;
}`)}
  <p><b>HTML-фрагмент:</b></p>
  ${codeBlock(`<nav class="nav">
  <ul class="nav__list">
    <li><a href="#about" class="nav__link">Про нас</a></li>
    <li><a href="#catalog" class="nav__link">Каталог</a></li>
    <li><a href="#contact" class="nav__link">Контакти</a></li>
  </ul>
</nav>`)}
  <p>
    Селектор <code>.nav &gt; .nav__list &gt; li &gt; a</code> дозволяє стилізувати лише ті посилання,
    які є безпосередніми нащадками структури меню всередині <code>.nav</code>.
  </p>

  <h3>4.4. Сусідні селектори</h3>
  <p>
    Сусідній селектор (adjacent sibling) застосовано для налаштування відступів
    між пунктами меню:
  </p>
  ${codeBlock(`.nav__list li + li {
  margin-left: 1.5rem;
}`)}
  <p>
    Селектор <code>li + li</code> задає відступ тільки для тих елементів <code>li</code>,
    у яких є попередній сусід. Завдяки цьому перший пункт меню не має зайвого лівого відступу.
  </p>

  <h3>4.5. Псевдокласи :hover та :focus</h3>
  <p>Псевдокласи використовуються для відгуку інтерфейсу на дії користувача.</p>

  <h4>Кнопки з псевдокласом :hover</h4>
  ${codeBlock(`.btn {
  border: 0;
  padding: .5rem 1rem;
  border-radius: 999px;
  background: #eee;
  cursor: pointer;
  transition: .2s;
  font-weight: 500;
}
.btn:hover {
  transform: translateY(-1px);
  box-shadow: var(--shadow);
}
.btn--primary {
  background: var(--brand);
  color: #fff;
}
.btn--primary:hover {
  background: var(--brand-dark);
}`)}

  <h4>Псевдоклас :focus для полів вводу (форма авторизації)</h4>
  ${codeBlock(`.input {
  width: 100%;
  border: 0;
  border-radius: 999px;
  padding: .7rem 1rem;
  background: #efefef;
  color: #111;
  font-size: .95rem;
  outline: none;
}
.input::placeholder { color: #6f6f6f; }
.input:focus { outline: 3px solid #cfe9d7; }`)}

  <p>
    При фокусі поле підсвічується м’яким контуром, що покращує UX і робить форму більш доступною.
  </p>

  <h3>4.6. Псевдоелементи</h3>
  <p>Псевдоелемент <code>::before</code> застосовано для кастомних маркерів списку в блоці «Чому варто обирати нас»:</p>
  ${codeBlock(`#about ul {
  list-style: none;
  margin: 1rem 0 0;
  padding: 0;
}

#about li {
  position: relative;
  padding-left: 1.5rem;
  margin-bottom: 0.6rem;
  font-size: 1.05rem;
  color: #333;
}

#about li::before {
  content: "✔";
  position: absolute;
  left: 0;
  top: 0;
  color: #2fb06b;
  font-size: 0.95rem;
  line-height: 1.2;
}`)}

  <p><b>HTML-фрагмент:</b></p>
  ${codeBlock(`<section id="about" class="home-section container">
  <h2>Чому варто обирати нас</h2>
  <ul>
    <li>Перевірена сумісність комплектуючих.</li>
    <li>Швидка доставка по Україні.</li>
    <li>Технічна підтримка 24/7.</li>
  </ul>
</section>`)}
  <p>
    Стандартні маркери списку приховані, натомість перед кожним пунктом
    з’являється зелена галочка, створена через псевдоелемент.
  </p>
  `,
  // 5–6. CSS: Шрифти, текст, таблиці, фон, контур, списки, CSS Pros
"css-pack": `
  <h2>5–6. CSS: Шрифти, Текст, Таблиці, Фон, Контур, Списки, CSS Pros</h2>
  <p>
    Відповідно до пункту 5 завдання, у проєкті виконано стилізацію текстових елементів,
    фону, контурів, таблиць, списків, а також використано «просунутий» CSS
    (змінні, тіні, transition, адаптивну верстку).
  </p>

  <h3>5.1. Шрифти, текст, кольори, фон</h3>
  <p>Базові налаштування шрифтів і кольорів оформлено через змінні <code>:root</code> та стилі для <code>body</code> і заголовків:</p>
  ${codeBlock(`:root {
  --brand: #2ea44f;
  --brand-dark: #1f6c35;
  --bg: #f9f9f9;
  --shadow: 0 4px 20px rgba(0,0,0,.08);
  --radius: 1rem;
}

body {
  margin: 0;
  font-family: system-ui, sans-serif;
  color: #111;
  background: var(--bg);
  line-height: 1.6;
}

/* заголовок hero-блоку */
.hero__title {
  font-size: clamp(1.8rem, 3.8vw, 2.8rem);
  margin: 0;
  color: #0c3;
}

/* заголовки секцій */
.section-title {
  margin: 0 0 .4rem;
  font-size: clamp(1.4rem, 2.5vw, 1.8rem);
  text-align: center;
}

.section-subtitle {
  color: #666;
  text-align: center;
  margin-bottom: 1.5rem;
}`)}
  <p>
    Тут показано використання змінних для кольорів, системного шрифту, а також
    функції <code>clamp()</code> для адаптивного розміру заголовків.
  </p>

  <h3>5.2. Фон, рамки, контури, тіні</h3>
  <p>Для виділення важливих блоків використано фон, рамки, радіус скруглення та тінь:</p>
  ${codeBlock(`.features {
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
  list-style: none;
  margin: 1rem 0;
  padding: 0;
}

.features > li {
  flex: 1 1 30%;
  background: #fff;
  border: 1px solid #eee;
  border-radius: .8rem;
  padding: 1rem;
  box-shadow: var(--shadow);
}

.features > li h3 {
  margin: .1rem 0 .3rem;
  color: var(--brand-dark);
}`)}
  <p>
    Блоки з перевагами мають білий фон, світлу рамку, скруглені кути та м’яку тінь,
    що створює акуратні «картки» сервісних можливостей.
  </p>

  <h3>5.3. Оформлення таблиць</h3>
  <p>Таблиця популярних товарів стилізована через окремі класи:</p>
  ${codeBlock(`.component-table-section {
  margin: 4rem auto;
  max-width: 1100px;
}

.component-table {
  width: 100%;
  border-collapse: collapse;
  background-color: #fff;
  font-size: 0.95rem;
  border-radius: 12px;
  overflow: hidden;
  box-shadow: 0 6px 16px rgba(0,0,0,0.06);
}

.component-table th,
.component-table td {
  padding: 1rem 1.2rem;
  border-bottom: 1px solid #e5e5e5;
  text-align: left;
}

.component-table thead {
  background-color: #111;
  color: #fff;
}

.component-table tbody tr:nth-child(even) {
  background-color: #fafafa;
}

.component-table tbody tr:hover {
  background-color: #ececec;
  transition: background-color 0.15s ease;
}`)}

  <p><b>HTML-фрагмент:</b></p>
  ${codeBlock(`<section class="component-table-section">
  <h2 class="section-title">Most popular products</h2>
  <table class="component-table">
    <thead>
      <tr>
        <th>Product</th>
        <th>Category</th>
        <th>Rating</th>
        <th>Monthly orders</th>
        <th>Short description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>AMD Ryzen 5 5600</td>
        <td>CPU</td>
        <td>⭐ 4.8 / 5</td>
        <td>120+</td>
        <td>Best budget 6-core CPU for gaming and everyday use.</td>
      </tr>
      <!-- інші рядки таблиці -->
    </tbody>
  </table>
</section>`)}
  <p>
    У таблиці використано різні стилі для <code>thead</code> та <code>tbody</code>,
    чергування фону рядків <code>nth-child(even)</code> і підсвітку рядка при наведенні.
  </p>

  <h3>5.4. Списки та їх оформлення</h3>
  <p>У проєкті є як текстові списки, так і списки-картки:</p>
  ${codeBlock(`#about ul {
  list-style: none;
  margin: 1rem 0 0;
  padding: 0;
}

#about li {
  position: relative;
  padding-left: 1.5rem;
  margin-bottom: 0.6rem;
  font-size: 1.05rem;
  color: #333;
}

#about li::before {
  content: "✔";
  position: absolute;
  left: 0;
  top: 0;
  color: #2fb06b;
  font-size: 0.95rem;
  line-height: 1.2;
}`)}
  <p>
    У блоці <code>#about</code> стандартні маркери списку вимкнені, а замість них
    за допомогою псевдоелемента <code>::before</code> відображається зелена галочка.
  </p>

  <h3>5.5. CSS «просунутий» (transition, тіні, адаптив, flex)</h3>
  <p>
    Для покращення UX застосовано transition, тіні та адаптивну верстку через медіа-запити.
    Нижче наведено фрагменти з оформлення карток каталогу:
  </p>
  ${codeBlock(`.catalog {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 1rem;
  list-style: none;
  margin: 0;
  padding: 0;
}

.card {
  flex: 1 1 calc(25% - 1rem);
  min-width: 240px;
  transition: transform .2s ease, box-shadow .2s ease;
}

.card__link {
  display: block;
  background: #fff;
  border-radius: var(--radius);
  padding: 1.2rem;
  text-decoration: none;
  color: inherit;
  text-align: center;
  box-shadow: var(--shadow);
  height: 100%;
}

.card__link:hover {
  transform: translateY(-5px);
  box-shadow: 0 10px 25px rgba(0,0,0,.1);
}`)}

  <p>Картки плавно піднімаються і отримують більшу тінь при наведенні курсора.</p>

  <h4>Адаптивна верстка через медіа-запити</h4>
  ${codeBlock(`@media (max-width: 1024px) {
  .card { flex-basis: calc(33.333% - 1rem); }
}
@media (max-width: 768px) {
  .card { flex-basis: calc(50% - 1rem); }
  .nav {
    position: fixed;
    top: 56px;
    left: 0;
    right: 0;
    background: #111;
    transform: translateY(-120%);
    transition: .25s;
  }
  .nav[data-open="true"] {
    transform: translateY(0);
  }
  .nav__list {
    flex-direction: column;
    padding: 1rem;
  }
}
@media (max-width: 480px) {
  .card { flex-basis: 100%; }
  .card__icon { width: 64px; height: 64px; }
}`)}

  <p>
    На широкому екрані картки відображаються по 4 в ряд, на планшетах — по 2–3,
    а на вузьких мобільних — по одній. Панель навігації перетворюється на
    мобільне меню, яке показується/ховається через атрибут <code>data-open</code>.
  </p>
`,
  "final": `<h2>Висновки</h2>

<p>
  У ході виконання практичної роботи №2 було застосовано основні та додаткові селектори CSS:
  селектори тегів, класів, ідентифікаторів, а також універсальні, дочірні, сусідні та селектори атрибута.
  Для кожного з них у звіт було додано HTML-код, CSS-код та приклади відображення в браузері.
</p>

<p>
  Було виконано стильове оформлення текстових елементів, списків, таблиць, карток та блоків інтерфейсу.
  Застосовано сучасні можливості CSS, такі як змінні <code>:root</code>, тіні, радіуси, адаптивність,
  transitions та hover-ефекти.
</p>

<p>
  У результаті роботи було закріплено навички організації стилів у проєкті,
  роботи з різними типами селекторів та створення охайного, структурованого й адаптивного інтерфейсу.
  Практична робота виконана повністю та відповідає вимогам методичних рекомендацій.
</p>
  `
  };

  const lab3 = {
    // Тема/мета/розташування
    theme3: `
      <h2>Практична робота №3 — Тема, мета, розташування</h2>

      <p><b>Тема лабораторної роботи:</b>
      РОЗРОБЛЕННЯ АДАПТИВНОГО ІНТЕРФЕЙСУ ВЕБ-ЗАСТОСУНКУ. МЕДІА-ЗАПИТИ. FLEXBOX/GRID. АНІМАЦІЇ.</p>

      <p><b>Мета лабораторної роботи:</b>
      набути практичних навичок створення адаптивної навігації (бургер-меню),
      адаптивної сітки контенту, використання медіа-запитів та анімацій.</p>

      <p><b>Тема розробки:</b> адаптивний веб-застосунок <i>«Конфігуратор ПК»</i>.</p>

      <h3 class="muted">Розташування (репозиторії)</h3>
  <div class="link-buttons">
    <a class="btn" href="https://github.com/MaixmK/IK-33_appWEB-Kutaiev-Maksym-FIOT-2025" target="_blank" rel="noopener">Репозиторій застосунку</a>
    <a class="btn" href="https://maixmk.github.io/IK-33_appWEB-Kutaiev-Maksym-FIOT-2025/" target="_blank" rel="noopener">Жива сторінка застосунку</a>
    <a class="btn" href="https://github.com/MaixmK/IK-33_appRECORD-Kutaiev-Maksym-FIOT-2025" target="_blank" rel="noopener">Репозиторій звітів</a>
    <a class="btn" href="https://maixmk.github.io/IK-33_appRECORD-Kutaiev-Maksym-FIOT-2025/" target="_blank" rel="noopener">Жива сторінка звітв</a>
  </div>
    `,
    site:`
    <a class="btn" href="https://github.com/MaixmK/IK-33_appWEB-Kutaiev-Maksym-FIOT-2025" target="_blank" rel="noopener">
          Репозиторій власного веб-застосунку
        </a>
    <a class="btn" href="https://maixmk.github.io/IK-33_appWEB-Kutaiev-Maksym-FIOT-2025/" target="_blank" rel="noopener">
          Жива сторінка власного сайту
    </a>
    <img src="img/site.jpg" width = 100% alt="Скрін головної сторінки веб-застосунку">
    `,
    // ===== Завдання №2 – фіксована таблична верстка =====
    tableFixed: `
<!DOCTYPE html>
<html>
      <head>
        <title> Фіксована таблична верстка </title>
        <link rel="stylesheet" type="text/css" href="style.css">
        <style>
        body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    font-size: 32px;
    }

    table {
        border-collapse: collapse;
        margin: 0 auto;
        margin-top: 10px;
        width: 800px;
    }

    td {
        padding: 5px;
        border: 2px solid black;
        text-align: center;
    }

    .cont2345 {
        display: flex;
    }

    .inner-table {
        height: 50px;
        width: 780px;
        border: 2px;
    }

    .inrercontainer {
        background-color: rgb(245, 253, 255);
        height: 50px;
        width: 220px;
    }

    .innrefist {
        border-color:  rgb(255, 233, 35);;
        background-color: rgb(255, 233, 35);
        text-align: right;
        height: 50px;
        padding-right: 150px;
        width: 300px;
    }

    .first {
        background-color: rgb(255, 233, 35);
        height: 100px;
        text-align: right;
    }

    .two {
        background-color: rgb(73, 119, 255);
        height: 550px;
        width: 180px;
        margin-right: 20px;
    }

    .tree {
        background-color: rgb(232, 71, 71);
        height: 550px;
        width: 180px;
        margin-right: 20px
    }

    .four {
        background-color: rgb(255, 245, 245);
        height: 550px;
        width: 180px;
        margin-right: 20px;
    }

    .five {
        background-color: rgb(232, 71, 71);
        height: 550px;
        width: 180px;
    }

    .six {
        background-color: rgb(255, 233, 35);
        height: 100px;
        text-align: left;
        padding-left: 150px;
    }
          </style>
          </head>
          <body>
            <table border =  "1" cellpadding = "5">
              <tr>
                <td class="first" colspan =  "5">
                    <table class="inner-table">
                        <tr>
                            <td class="inrercontainer"> </td>
                            <td class="innrefist"> 1 </td>
                        </tr>
                    </table>
                </td>
              </tr>
              <tr class="cont2345">
                <td class="two"> 2 </td>
                <td class="tree"> 3 </td>
                <td class="four"> 4 </td>
                <td class="five"> 5 </td>
              </tr>
              <tr>
                <td class="six" colspan =  "4"> 6 </td>
              </tr>
            </table>
          </body>
    </html>
    `,

    // ===== Завдання №2 – гумова таблична верстка =====
    tableFluid: `
      <h2>Завдання №2 — таблична верстка (гумова)</h2>
      <p>Версія макета з <b>гумовою табличною версткою</b> — ширина в %, макс. ширина обмежена.</p>

      <!DOCTYPE html>
<html>
      <head>
        <title> Гумова таблична верстка </title>
        <link rel="stylesheet" type="text/css" href="style.css">
        <style>
        body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    font-size: 32px;
}

table {
    border-collapse: collapse;
    margin: 10px;
    width: 99%;
    height: 96vh;
}

td {
    padding: 5px;
    border: 2px solid black;
    text-align: center;
}

.cont2345 {
    display: flex;
    height: 100%;
}

.inner-table {
    height: 70%;
    border: 2px;
}

.inrercontainer {
    background-color: rgb(245, 253, 255);
    width: 20%;
}

.innrefist {
    border-color:  rgb(255, 233, 35);
    background-color: rgb(255, 233, 35);
    text-align: right;
    padding-right: 150px;
    width: 80%;
}

.first {
    background-color: rgb(255, 233, 35);
    height: 15%;
    text-align: right;
}

.two {
    background-color: rgb(73, 119, 255);
    height: 98%;
    width: 25%;
    margin-right: 20px;
}

.tree {
    background-color: rgb(232, 71, 71);
    height: 98%;
    width: 25%;
    margin-right: 20px
}

.four {
    background-color: rgb(255, 245, 245);
    height: 98%;
    width: 25%;
    margin-right: 20px;
}

.five {
    background-color: rgb(232, 71, 71);
    height: 98%;
    width: 25%;
}

.six {
    background-color: rgb(255, 233, 35);
    height: 15%;
    text-align: left;
    padding-left: 150px;
}
      </style>
      </head>
      <body>
        <table border =  "1" cellpadding = "5">
          <tr>
            <td class="first" colspan =  "5">
                <table class="inner-table">
                    <tr>
                        <td class="inrercontainer"> </td>
                        <td class="innrefist"> 1 </td>
                    </tr>
                </table>
            </td>
          </tr>
          <tr class="cont2345">
            <td class="two"> 2 </td>
            <td class="tree"> 3 </td>
            <td class="four"> 4 </td>
            <td class="five"> 5 </td>
          </tr>
          <tr>
            <td class="six" colspan =  "4"> 6 </td>
          </tr>
        </table>
      </body>
</html>
    `,

    // ===== Завдання №2 – фіксована блокова (float) =====
    floatFixed: `
      <h2>Завдання №1 — блокова верстка (фіксована, float)</h2>
      <p>Той самий макет, але реалізований за допомогою <b>блоків із плаваючими елементами (float)</b>.</p>


      <head>
        <title> Фіксована блокова верстка </title>
        <link rel="stylesheet" type="text/css" href="style.css">
        <style>
        
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    font-size: 32px;
}

.mainblock {
    margin: 0 auto;
    margin-top: 10px;
    width: 790px;
    border: 2px solid black;
}

.topblock {
    background-color: rgb(255, 233, 35);
    border: 2px solid black;
    height: 100px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-left: 20px;
    padding-right: 70px;
}

.inercontainer {
    background-color: rgb(245, 253, 255);
    border: 2px solid black;
    height: 50px;
    width: 200px;
}

.first {
    text-align: right;
    height: 50px;
    padding-right: 150px;
    width: 300px;
}

.cont2345 {
    display: flex;
}



.two {
    background-color: rgb(73, 119, 255);
    height: 550px;
    width: 180px;
    margin-right: 20px;
    border: 2px solid black;
    text-align: center;
}

.tree {
    background-color: rgb(232, 71, 71);
    border: 2px solid black;
    height: 550px;
    width: 180px;
    margin-right: 20px;
    text-align: center;
}

.four {
    background-color: rgb(255, 245, 245);
    border: 2px solid black;
    height: 550px;
    width: 180px;
    margin-right: 20px;
    text-align: center;
}

.five {
    background-color: rgb(232, 71, 71);
    border: 2px solid black;
    height: 550px;
    width: 180px;
    text-align: center;
}

.six {
    background-color: rgb(255, 233, 35);
    border: 2px solid black;
    height: 100px;
    text-align: center;
    padding-left: 100px;
    display: flex;
    align-items: center;
    justify-content: left;
}
          </style>
      </head>
      <body>

        <div class="mainblock">
            <div class="topblock">
                <div class="inercontainer"> </div>
                1
            </div>
            
            <div class="cont2345">
                <div class="two">2</div>
                <div class="tree">3</div>
                <div class="four">4</div>
                <div class="five">5</div>
            </div>
            
            <div class="six">6</div>
            
            </body>
        </div>

    `,

    // ===== Завдання №2 – гумова блокова (float) =====
    floatFluid: `
      <h2>Завдання №1 — блокова верстка (гумова, float)</h2>
      <p>Гумова версія блокової верстки — контейнер тягнеться на 100% ширини з обмеженням max-width.</p>

      <!DOCTYPE html>
<html>
      <head>
        <title> Гумова блокова верстка </title>
        <link rel="stylesheet" type="text/css" href="style.css">
        <style>
        
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    font-size: 32px;
}

.mainblock {
    margin: 0 auto;
    margin-top: 10px;
    width: 99%;
    height: 96vh;
    border: 2px solid black;
}

.topblock {
    background-color: rgb(255, 233, 35);
    border: 2px solid black;
    height: 15%;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-left: 20px;
    padding-right: 70px;
}

.inercontainer {
    background-color: rgb(245, 253, 255);
    border: 2px solid black;
    height: 60%;
    width: 20%;
}

.first {
    text-align: right;
    height: 50px;
    padding-right: 150px;
    width: 300px;
}

.cont2345 {
    display: flex;
    height: 70%;
}



.two {
    background-color: rgb(73, 119, 255);
    height: 100%;
    width: 25%;
    margin-right: 20px;
    border: 2px solid black;
    text-align: center;
}

.tree {
    background-color: rgb(232, 71, 71);
    border: 2px solid black;
    height: 100%;
    width: 25%;
    margin-right: 20px;
    text-align: center;
}

.four {
    background-color: rgb(255, 245, 245);
    border: 2px solid black;
    height: 100%;
    width: 25%;
    margin-right: 20px;
    text-align: center;
}

.five {
    background-color: rgb(232, 71, 71);
    border: 2px solid black;
    height: 100%;
    width: 25%;
    text-align: center;
}

.six {
    background-color: rgb(255, 233, 35);
    border: 2px solid black;
    height: 15%;
    text-align: center;
    padding-left: 100px;
    display: flex;
    align-items: center;
    justify-content: left;
}
          </style>
      </head>
      <body>

        <div class="mainblock">
            <div class="topblock">
                <div class="inercontainer"> </div>
                1
            </div>
            
            <div class="cont2345">
                <div class="two">2</div>
                <div class="tree">3</div>
                <div class="four">4</div>
                <div class="five">5</div>
            </div>
            
            <div class="six">6</div>
            
            </body>
        </div>
</html>
    `,

    z3:`
    Виконане завдання №3
    <a class="btn" href="https://github.com/MaixmK/Web-lab3-task3-main-main" target="_blank" rel="noopener">
         Репозиторій сайту
    </a>
    <a class="btn" href="https://maixmk.github.io/Web-lab3-task3-main-main/" target="_blank" rel="noopener">
         Жива сторінка сайту
    </a>
    <img src="img/z3.jpg" width = 100% alt="Скрін завдання №3">
    `,
    final:`
    У ході виконання лабораторної роботи №3 я отримав комплексне розуміння принципів верстки веб-інтерфейсів та їх адаптації під різні типи пристроїв. Було опрацьовано декілька підходів до побудови макетів: таблична верстка, блокова верстка з використанням плаваючих елементів та сучасна адаптивна верстка на основі Flexbox. Для кожного з підходів я створив окремі реалізації як у фіксованому, так і в гумовому варіантах, що дозволило порівняти їхні переваги та недоліки.

Окрему увагу я приділив створенню адаптивного дизайну, використавши систему медіа-запитів та логічні оператори CSS. Для десктопної, планшетної та мобільної версій були розроблені індивідуальні правила стилізації, які забезпечують коректне відображення інтерфейсу на різних розмірах екранів. Також реалізовано адаптивне навігаційне меню (бургер-меню), що автоматично активується на малих ширинах пристрою.
    `,
  };

  const lab4 = {
  // 4.1 Тема, мета, розташування
  theme4: `
    <h2>Практична робота №4 — Тема, мета, розташування</h2>

    <p><b>Тема практичної роботи:</b>
      ФУНКЦІОНАЛЬНЕ ЗАСТОСУВАННЯ JAVASCRIPT У HTML-ДОКУМЕНТІ. УМОВНІ ОПЕРАТОРИ, ФУНКЦІЇ, МАСИВИ.
    </p>

    <p><b>Мета практичної роботи:</b>
      набути практичних навичок використання мови JavaScript в HTML-документі:
      роботи з умовами, циклами, функціями, масивами, а також створення
      невеликого інтерактивного застосунку (калькулятора).
    </p>

    <p><b>Тема розробки:</b> реалізація набору скриптів для розв’язання задач 1–8
      та інтерактивного калькулятора (завдання 9).
    </p>

    <h3 class="muted">Розташування</h3>

    <div class="link-buttons">
      <a class="btn" href="https://github.com/MaixmK/IK-33_appWEB-Kutaiev-Maksym-FIOT-2025" target="_blank" rel="noopener">
        Репозиторій WEB-застосунку
      </a>
      <a class="btn" href="https://maixmk.github.io/IK-33_appWEB-Kutaiev-Maksym-FIOT-2025/" target="_blank" rel="noopener">
        Жива сторінка WEB-застосунку
      </a>
      <a class="btn" href="https://github.com/MaixmK/IK-33_appRECORD-Kutaiev-Maksym-FIOT-2025" target="_blank" rel="noopener">
        Репозиторій звітного HTML
      </a>
      <a class="btn" href="https://maixmk.github.io/IK-33_appRECORD-Kutaiev-Maksym-FIOT-2025/" target="_blank" rel="noopener">
        Жива сторінка звітів
      </a>
    </div>
  `,

  // 4.2 Короткий опис усіх задач
  "tasks-common": `
    <h2>Перелік завдань практичної роботи №4</h2>
    <ol>
      <li>Визначення знака введеного числа (додатнє, від’ємне, нуль).</li>
      <li>Використання конструкції <code>switch</code> для визначення пори року за номером.</li>
      <li>Реалізація авторизації для користувачів Admin / User з перевіркою пароля.</li>
      <li>Функції <code>makeTransaction</code>, <code>getShippingMessage</code> та ін.</li>
      <li>Функція <code>checkForSpam()</code> та перевірка наявності слова <em>spam/sale</em> у рядку.</li>
      <li>Робота з масивами: <code>filterArray()</code>, <code>makeArray()</code>.</li>
      <li>Операції над 1D масивами: пошук максимуму/мінімуму, перестановка, сортування вставками.</li>
      <li>Робота з двовимірним масивом, розбиття на додатні та від’ємні елементи.</li>
      <li>Створення інтерактивного калькулятора з основними арифметичними операціями, відсотком, коренем та піднесенням до степеня.</li>
    </ol>
    <p>Далі для кожного завдання наведено фрагмент коду та приклад результату.</p>
  `,

  task1: `
    <h2>Завдання 1 — визначення знака числа</h2>

    <h3>Умова</h3>
    <pre>
Ввести число через prompt, зберегти у змінну, вивести його у console.
Перевірити: чи є число додатним, від’ємним або дорівнює нулю.
Вивести результат за допомогою alert().
    </pre>

    <h3>Фрагмент коду</h3>
    ${codeBlock(`
  function task1() {
  const valueStr = prompt("Введіть число (може бути від’ємним, додатним або 0):");

  if (valueStr === null) {
    alert("Ввід скасовано");
    console.log("Ввід скасовано");
    return;
  }

  const value = Number(valueStr);

  if (Number.isNaN(value)) {
    alert("Помилка: введено не число");
    console.log("Помилка: введено не число");
    return;
  }

  console.log(\`Ви ввели число: \${value}\`);

  if (value > 0) {
    alert("Число додатнє");
    console.log("Число додатнє");
  } else if (value < 0) {
    alert("Число від’ємне");
    console.log("Число від’ємне");
  } else {
    alert("Число дорівнює нулю");
    console.log("Число дорівнює нулю");
  }
}`)}

    <h3>Результат</h3>
    <p>Скріншот консолі:</p>
    <img src="lab4/1z.png" alt="Console для завдання 1" />
    <img src="lab4/1z(1).png" alt="Console для завдання 1" />
    <img src="lab4/1z(2).png" alt="Console для завдання 1" />
  `,


  task2: `
  <h2>Завдання 2 — номер сезону (switch)</h2>

  <h3>Умова</h3>
  <pre>
Змінна number може набувати 4 значення: '1', '2', '3' або '4' 
(запитуй це значення у користувача через prompt). 
 
Якщо вона має значення '1', то у змінну result запишіть 'зима',
якщо має значення '2' - 'весна' і так далі.
Розв'яжіть завдання через switch-case.
Не забудьте про дефолтне значення, на випадок, якщо користувач введе в prompt щось інше. 
Значення змінної result виведіть в консоль.
  </pre>

  <h3>Фрагмент коду</h3>
  ${codeBlock(`function task2() {
  const number = prompt("Введіть число від 1 до 4 (1-зима, 2-весна, 3-літо, 4-осінь):");

  let result;

  switch (number) {
    case "1":
      result = "зима";
      break;
    case "2":
      result = "весна";
      break;
    case "3":
      result = "літо";
      break;
    case "4":
      result = "осінь";
      break;
    default:
      result = "невірне значення, очікувалось 1–4";
  }

  console.log("Результат для number=" + number + ": " + result);
}`)}


  <h3>Результат (скріншот Console)</h3>
  <img src="lab4/2z.png" alt="Console завдання 2">

  <img src="lab4/2z(1).png" alt="Console завдання 2">

  <img src="lab4/2z(2).png" alt="Console завдання 2">
`,

  task3: `
  <h2>Завдання 3 — Admin та User з паролями</h2>

  <h3>Умова</h3>
  <pre>
Є Admin  та User, з відповідними паролями.   (Об’явити , проініцілізувати) 

Написати програмний код, який запитуватиме у користувача  логін (використати  prompt) 
і виводить результат в консоль браузера, використовуючи шаблонний рядок.

 Після цього додайте перевірку введеного користувачем значення: 
	якщо відвідувач вводить " Admin  ",  то prompt запитує пароль. 
  У випадку вдалої ідентифікації в alert вивести « Hello, Admin  ;
	якщо нічого не ввели або натиснули Cancel, то вивести в alert рядок  " Cancelled ";
	в іншому випадку вивести в alert рядок " I don't know you"

Теж саме виконати з User

  </pre>

  <h3>Фрагмент коду</h3>

  ${codeBlock(`const ADMIN_LOGIN = "Admin";
const ADMIN_PASSWORD = "admin123";

const USER_LOGIN = "User";
const USER_PASSWORD = "user123";

function loginAdminOrUser() {
  const login = prompt("Введіть логін (Admin або User):");

  console.log(\`Користувач ввів логін: \${login}\`);

  if (login === null || login === "") {
    alert("Cancelled");
    console.log("→ Ввід логіну скасовано");
    return;
  }

  if (login === ADMIN_LOGIN) {
    console.log("Розпізнано користувача: Admin");
    const password = prompt("Введіть пароль для Admin:");
    console.log(\`Admin ввів пароль: \${password}\`);

    if (password === null || password === "") {
      alert("Cancelled");
      console.log("→ Ввід пароля скасовано");
    } else if (password === ADMIN_PASSWORD) {
      alert("Hello, Admin");
      console.log("→ Успішний вхід як Admin");
    } else {
      alert("I don't know you");
      console.log("→ Невірний пароль для Admin");
    }

  } else if (login === USER_LOGIN) {
    console.log("Розпізнано користувача: User");
    const password = prompt("Введіть пароль для User:");
    console.log(\`User ввів пароль: \${password}\`);

    if (password === null || password === "") {
      alert("Cancelled");
      console.log("→ Ввід пароля скасовано");
    } else if (password === USER_PASSWORD) {
      alert("Hello, User");
      console.log("→ Успішний вхід як User");
    } else {
      alert("I don't know you");
      console.log("→ Невірний пароль для User");
    }

  } else {
    alert("I don't know you");
    console.log(\`→ Невідомий логін: \${login}\`);
  }
}`)}

<h3>Результат (скріншот Console)</h3>
  <img src="lab4/3z.png" alt="Console завдання 2">
  <img src="lab4/3z(1).png" alt="Console завдання 2">
  <img src="lab4/3z(2).png" alt="Console завдання 2">
  <img src="lab4/3z(3).png" alt="Console завдання 2">
  `,

task4: `
  <h2>Завдання 4</h2>

  <h3>Умова</h3>
  <pre>
Станція з продажу ремонтних дроїдів.

Оголоси функцію makeTransaction, яка очікує два параметри, значення яких будуть задаватися під час її виклику: 
• quantity— перший параметр, число, що містить кількість замовлених дроїдів • pricePerDroid — другий параметр, 
число, що містить вартість одного дроїда

Напиши  код функції так, щоб вона повертала рядок з повідомленням про покупку ремонтних дроїдів: 
"You ordered <quantity> droids worth <totalPrice> credits!", де:
• <quantity> — це кількість замовлених дроїдів 
• <totalPrice> — це загальна вартість замовлення, тобто вартість усіх замовлених дроїдів
  </pre>

  <h3>Фрагмент коду</h3>
    ${codeBlock(`// ===== ЗАВДАННЯ 4: makeTransaction =====

function makeTransaction(quantity, pricePerDroid) {
  const totalPrice = quantity * pricePerDroid;
  return \`You ordered \${quantity} droids worth \${totalPrice} credits!\`;
}`)}

  <h3>Виклик функції</h3>
    ${codeBlock(`    // ЗАВДАННЯ 4
    function runTask4_1() {
      clearAndTitle("Завдання 4.1: makeTransaction");
      console.log(makeTransaction(3, 2000));
      console.log(makeTransaction(5, 1000));
    }`)}
  <h3>Результат (скріншот Console)</h3>
  <img src="lab4/4.png" alt="Console завдання 4">
`,

task5: `
  <h2>Завдання 5 — Перевірка на спам</h2>

  <h3>Умова</h3>
  <pre>
Функція checkForSpam(message) приймає рядок (параметр message), 
перевіряє його на вміст заборонених слів spam і sale, і повертає результат перевірки. 
Слова в рядку параметра message можуть бути в довільному регістрі, наприклад SPAM або sAlE.

Доповни код функції таким чином, що:
•	Якщо знайдено заборонене слово (spam або sale), то функція повертає буль true
•	Якщо в рядку відсутні заборонені слова, функція повертає буль false

  </pre>

  <h3>Фрагмент коду</h3>
  ${codeBlock(`// ===== ЗАВДАННЯ 5.1: checkForSpam =====
function checkForSpam(message) {
  const lower = message.toLowerCase();
  return lower.includes("spam") || lower.includes("sale");
}`)}

  <h3>Виклик функції</h3>
  ${codeBlock(`// ЗАВДАННЯ 5
function runTask5_1() {
  clearAndTitle("Завдання 5: checkForSpam");
  const msg = prompt("Введіть повідомлення для перевірки на spam/sale:");
  if (msg === null) return;
  console.log("Повідомлення:", msg);
  console.log("Містить спам:", checkForSpam(msg));
}`)}

  <h3>Результат (скріншот Console)</h3>
  <img src="lab4/5z.png" alt="Console завдання 5">
  <img src="lab4/5z(1).png" alt="Console завдання 5">
`,

task6: ` 
  <h2>Завдання 6 — filterArray</h2>

  <h3>Умова</h3>
  <pre>
Напиши функцію filterArray(numbers, value), яка приймає масив чисел (numbers)
та значення (value) як параметри. Функція повинна повертати новий масив лише тих чисел 
із масиву numbers, які більші за value.

Усередині функції:
• Створи порожній масив, у який будеш додавати підходящі числа.
• Використай цикл для ітерації кожного елемента масиву numbers.
• Використовуй умовний оператор if для перевірки елементів.
Поверни новий масив з усіма підходящими числами.
  </pre>

  <h3>Фрагмент коду</h3>
  ${codeBlock(`// ===== ЗАВДАННЯ 6.1: filterArray =====

function filterArray(numbers, value) {
  const result = [];

  for (let i = 0; i < numbers.length; i++) {
    if (numbers[i] > value) {
      result.push(numbers[i]);
    }
  }

  return result;
}`)}

  <h3>Виклик функції</h3>
  ${codeBlock(`// ЗАВДАННЯ 6.1
function runTask6_1() {
  clearAndTitle("Завдання 6.1: filterArray");
  console.log(filterArray([1, 5, 10, -2, 8], 3));
}`)})

  <h3>Результат (скріншот Console)</h3>
  <img src="lab4/6z.png" alt="Console завдання 6.1">
`,

task7: `
  <h2>Завдання 7 — Масив A, Масив B та сортування вставками</h2>

  <h3>Умова</h3>
  <pre>
1. Користувач задає розмір одновимірного масиву A.
   Масив A заповнюється випадковими числами.

2. Побудувати масив B за формулою:
      bᵢ = max * aᵢ
   де max — максимальний елемент масиву A.

3. Вивести вхідний масив A та вихідний масив B.

4. Виконати сортування масиву B методом вставки (за зменшенням).

5. Усі дії виконати через окремі функції.
  </pre>

  <h3>Фрагмент коду</h3>
  ${codeBlock(`// ===== ЗАВДАННЯ 7 =====

// Генерація одновимірного масиву
function generateArray(n) {
  const arr = [];
  for (let i = 0; i < n; i++) {
    arr.push(Math.floor(Math.random() * 20) - 10);
  }
  return arr;
}

// Пошук максимального елемента
function getMax(arr) {
  return Math.max(...arr);
}

// Побудова масиву B: bᵢ = max * aᵢ
function buildB(arrA, max) {
  const B = [];
  for (let i = 0; i < arrA.length; i++) {
    B.push(arrA[i] * max);
  }
  return B;
}

// Сортування вставками за зменшенням
function insertionSortDesc(arr) {
  for (let i = 1; i < arr.length; i++) {
    let current = arr[i];
    let j = i - 1;

    while (j >= 0 && arr[j] < current) {
      arr[j + 1] = arr[j];
      j--;
    }

    arr[j + 1] = current;
  }
  return arr;
}

// Головна функція запуску
function runTaskArrayAB() {
  const n = Number(prompt("Введіть кількість елементів масиву A:"));

  if (!n || n <= 0) {
    alert("Некоректне значення");
    return;
  }

  const A = generateArray(n);
  console.log("Масив A:", A);

  const maxA = getMax(A);
  console.log("Максимальний елемент A:", maxA);

  const B = buildB(A, maxA);
  console.log("Масив B (до сортування):", B);

  const sortedB = insertionSortDesc([...B]);
  console.log("Масив B (після сортування вставками за зменшенням):", sortedB);
}

// Обгортка для звіту
function runTask7() {
  clearAndTitle("Завдання 7 – Масив A, Масив B, сортування вставками");
  runTaskArrayAB();
}`)}

  <h3>Виклик функції</h3>
  ${codeBlock(`    function runTask7() {
      clearAndTitle("Завдання 7: Масив A → Масив B → Сортування вставками");
      runTaskArrayAB();
    }`)}

  <h3>Результат (скріншот Console)</h3>
  <img src="lab4/7z.png" alt="Console завдання 7">
`,

task8: `
  <h2>Завдання 8 — двовимірний масив, додатні/від’ємні</h2>

  <h3>Умова</h3>
  <pre>
Є двовимірний масив додатніх і від’ємних чисел (оголосити і проініціалізувати
генератором випадкових чисел). Вивести його у Console браузера.

Створити два масиви:
• масив додатніх чисел
• масив від’ємних чисел

Третій елемент у додатньому масиві замінити на від’ємне значення,
яке буде введено за допомогою prompt().

Після заміни вивести отримані масиви у Console браузера.
  </pre>

  <h3>Фрагмент коду</h3>
  ${codeBlock(`// Генерація двовимірного масиву випадкових чисел
function generate2DArray(rows, cols, min = -10, max = 10) {
  const matrix = [];
  for (let i = 0; i < rows; i++) {
    const row = [];
    for (let j = 0; j < cols; j++) {
      const value = Math.floor(Math.random() * (max - min + 1)) + min;
      row.push(value);
    }
    matrix.push(row);
  }
  return matrix;
}

// ===== ЗАВДАННЯ 8 =====
function task8() {
  const rows = 3;
  const cols = 4;
  const matrix = generate2DArray(rows, cols);

  console.log("Початковий двовимірний масив:");
  console.log(matrix);

  const positives = [];
  const negatives = [];

  for (let i = 0; i < matrix.length; i++) {
    for (let j = 0; j < matrix[i].length; j++) {
      const value = matrix[i][j];
      if (value >= 0) {
        positives.push(value);
      } else {
        negatives.push(value);
      }
    }
  }

  console.log("Масив додатніх чисел:", positives);
  console.log("Масив від’ємних чисел:", negatives);

  if (positives.length >= 3) {
    const negValueStr = prompt(
      "Введіть від’ємне число для заміни третього елемента додатнього масиву:"
    );
    let negValue = Number(negValueStr);

    if (Number.isNaN(negValue) || negValue >= 0) {
      alert("Некоректне значення. Поставимо -1.");
      negValue = -1;
    }

    positives[2] = negValue;
  } else {
    console.log(
      "У масиві додатніх чисел менше 3 елементів — заміна неможлива."
    );
  }

  console.log(
    "Масив додатніх чисел після заміни третього елемента:",
    positives
  );
}`)}

  <h3>Виклик функції</h3>
  ${codeBlock(`// ЗАВДАННЯ 8
function runTask8() {
  clearAndTitle("Завдання 8: двовимірний масив");
  task8();
}`)}

  <h3>Результат (скріншот Console)</h3>
  <img src="lab4/8z.png" alt="Console завдання 8">
`,


  // 4.4 приклад для калькулятора (завдання 9)
  task9: `
  <h2>Завдання 9 — Калькулятор</h2>

  <h3>Умова</h3>
  <pre>
Реалізувати калькулятор мовою JavaScript.

Обов'язкові операції:
  • додавання (+)
  • віднімання (-)
  • множення (*)
  • ділення (/)
  • остача від ділення (%)
  • корінь квадратний (√)
  • піднесення до степеня (xʸ)

Кнопки з цифрами та знаками операцій мають бути доступні
в інтерфейсі вікна.
  </pre>

  <h3>HTML-розмітка калькулятора</h3>
  ${codeBlock(`<div id="calculator-icon">
  🧮
</div>

<div id="calculator-modal" class="hidden">
  <div class="modal-content">
    <span id="calc-close">&times;</span>

    <h3 style="text-align:center;">Калькулятор</h3>

    <input type="text" class="display" id="display" readonly>

    <div class="buttons">
      <button class="clear" id="clear">C</button>
      <button id="backspace">⌫</button>
      <button class="operator" data-op="%">%</button>

      <button data-num="7">7</button>
      <button data-num="8">8</button>
      <button data-num="9">9</button>
      <button class="operator" data-op="/">/</button>

      <button data-num="4">4</button>
      <button data-num="5">5</button>
      <button data-num="6">6</button>
      <button class="operator" data-op="*">*</button>

      <button data-num="1">1</button>
      <button data-num="2">2</button>
      <button data-num="3">3</button>
      <button class="operator" data-op="-">-</button>

      <button data-num="0">0</button>
      <button data-num=".">.</button>
      <button class="operator" data-op="+">+</button>
      <button class="operator" id="sqrt">√</button>

      <button class="operator" id="pow">xʸ</button>
      <button class="equal" id="equal">=</button>
    </div>
  </div>
</div>`)}  

  <h3>Фрагмент JavaScript-коду</h3>
  ${codeBlock(`// === ЛОГІКА КАЛЬКУЛЯТОРА ===
const display = document.getElementById("display");

let firstOperand = null;
let operator = null;
let waitingForSecondOperand = false;

function inputDigit(digit) {
  if (waitingForSecondOperand) {
    display.value = digit;
    waitingForSecondOperand = false;
  } else {
    display.value = display.value === "0" ? digit : display.value + digit;
  }
}

function inputDot() {
  if (waitingForSecondOperand) {
    display.value = "0.";
    waitingForSecondOperand = false;
    return;
  }
  if (!display.value.includes(".")) {
    display.value += ".";
  }
}

function handleOperator(nextOperator) {
  const inputValue = parseFloat(display.value);

  if (operator && waitingForSecondOperand) {
    operator = nextOperator;
    return;
  }

  if (firstOperand === null && !Number.isNaN(inputValue)) {
    firstOperand = inputValue;
  } else if (operator) {
    const result = calculate(firstOperand, inputValue, operator);
    display.value = String(result);
    firstOperand = result;
  }

  operator = nextOperator;
  waitingForSecondOperand = true;
}

function calculate(a, b, op) {
  switch (op) {
    case "+": return a + b;
    case "-": return a - b;
    case "*": return a * b;
    case "/": return b === 0 ? "Error" : a / b;
    case "%": return a % b;
    case "^": return Math.pow(a, b);
    default: return b;
  }
}

function clearAll() {
  display.value = "";
  firstOperand = null;
  operator = null;
  waitingForSecondOperand = false;
}

function backspace() {
  display.value = display.value.slice(0, -1);
}

// Обробка цифр
document.querySelectorAll("button[data-num]").forEach(btn => {
  btn.addEventListener("click", () => {
    const value = btn.getAttribute("data-num");
    if (value === ".") inputDot();
    else inputDigit(value);
  });
});

// Обробка операторів (+ - * / %)
document.querySelectorAll("button.operator[data-op]").forEach(btn => {
  btn.addEventListener("click", () => {
    handleOperator(btn.getAttribute("data-op"));
  });
});

// √
document.getElementById("sqrt").addEventListener("click", () => {
  const value = parseFloat(display.value);
  if (Number.isNaN(value)) return;

  if (value < 0) {
    display.value = "Error";
    return;
  }

  const result = Math.sqrt(value);
  display.value = String(result);

  firstOperand = result;
  operator = null;
  waitingForSecondOperand = false;
});

// xʸ
document.getElementById("pow").addEventListener("click", () => {
  const inputValue = parseFloat(display.value);
  if (Number.isNaN(inputValue)) return;

  if (firstOperand === null) {
    firstOperand = inputValue;
  } else if (operator) {
    const result = calculate(firstOperand, inputValue, operator);
    display.value = String(result);
    firstOperand = result;
  }

  operator = "^";
  waitingForSecondOperand = true;
});

// =
document.getElementById("equal").addEventListener("click", () => {
  const inputValue = parseFloat(display.value);

  if (operator === null || waitingForSecondOperand) return;

  const result = calculate(firstOperand, inputValue, operator);
  display.value = String(result);
  firstOperand = null;
  operator = null;
  waitingForSecondOperand = false;
});

document.getElementById("clear").addEventListener("click", clearAll);
document.getElementById("backspace").addEventListener("click", backspace);`)}

  <h3>Виклик/Інтеграція в інтерфейс</h3>
  <pre>
Іконка 🧮 відображається у правому нижньому куті сторінки.
При кліку по ній відкривається модальне вікно з калькулятором.
Кнопки з цифрами та операціями керують вмістом поля display.
  </pre>

  <h3>Результат (скріншот інтерфейсу)</h3>
  <img src="lab4/calc.png" alt="Інтерфейс калькулятора, завдання 9">
`,


  // 4.5 Висновки
  final4: `
    <h2>Висновки по практичній роботі №4</h2>
    <p>
      У ході виконання роботи було реалізовано дев’ять задач мовою JavaScript:
      перевірку введеного числа, роботу з конструкцією <code>switch</code>,
      просту авторизацію, набір функцій для обробки рядків і масивів,
      а також обробку одновимірних і двовимірних масивів.
    </p>
    <p>
      Окремо реалізовано інтерактивний калькулятор, який демонструє
      використання обробників подій, роботи з DOM та базових арифметичних
      операцій в JavaScript. Отримані навички можна застосовувати при
      створенні більш складних веб-застосунків.
    </p>
  `
  };

  const lab5 = {
theme5: `
  <h2>Практична робота №5</h2>

  <h3>Тема</h3>
  <p>
    <b>ОБ'ЄКТ. МЕТОДИ ОБ'ЄКТА. МАСИВ ОБ'ЄКТІВ. ДЕСТРУКТУРИЗАЦІЯ ОБ'ЄКТІВ. CALLBACK.
    СТРІЛОЧНІ ФУНКЦІЇ. СТРІЛОЧНІ ФУНКЦІЇ ЯК КОЛБЕКИ. ПЕРЕБИРАЮЧІ МЕТОДИ МАСИВУ.</b>
  </p>

  <h3>Мета</h3>
  <p>
    Придбати практичні навички роботи з об'єктами, методами об'єкта,
    масивами об'єктів, callback-функціями, стрілочними функціями та перебираючими
    методами масиву. Реалізація програм засобами мови JavaScript.
  </p>

  <h3>Постановка задачі</h3>
  <p>
    У звітному HTML-документі розмістити тему та постановку задачі лабораторної
    роботи №5. Розмістити програмний код і результати виконання завдань
    згідно таблиці варіантів.  
    Студенти з непарними номерами виконують варіанти:
    <b>1, 3, 5, 7, 9, 10</b>.  
  </p>

  <p>
    Використовуючи сценарій JavaScript, виконати завдання згідно таблиці №1,
    а також завдання №2.
  </p>

    <h3 class="muted">Розташування</h3>

  <div class="link-buttons">
    <a class="btn" href="https://github.com/MaixmK/IK-33_appWEB-Kutaiev-Maksym-FIOT-2025" target="_blank" rel="noopener">Репозиторій застосунку</a>
    <a class="btn" href="https://maixmk.github.io/IK-33_appWEB-Kutaiev-Maksym-FIOT-2025/" target="_blank" rel="noopener">Жива сторінка застосунку</a>
    <a class="btn" href="https://github.com/MaixmK/IK-33_appRECORD-Kutaiev-Maksym-FIOT-2025" target="_blank" rel="noopener">Репозиторій звітів</a>
    <a class="btn" href="https://maixmk.github.io/IK-33_appRECORD-Kutaiev-Maksym-FIOT-2025/" target="_blank" rel="noopener">Жива сторінка звітв</a>
  </div>
`,


  task1_1: `
  <h2>Завдання 1.1 — createProduct та callback-функції</h2>

  <h3>Умова</h3>
  <pre>
Напишіть наступні функції:
createProduct(obj, callback) - приймає об'єкт товару без id, а також коллбек. 
Функція створює об'єкт товару, додаючи йому унікальний ідентифікатор у 
властивість id та викликає коллбек передаючи йому створений об'єкт.

logProduct(product) - колббек що приймає об'єкт продукту і логуючий його в консоль
logTotalPrice(product) - колббек, що приймає об'єкт продукту і логіює загальну вартість товару в консоль

  </pre>

  <h3>Фрагмент коду</h3>
  ${codeBlock(`// Лічильник для унікальних id
let productIdCounter = 1;

function createProduct(obj, callback) {
  const product = {
    ...obj,
    id: productIdCounter++,
  };

  callback(product);
}

function logProduct(product) {
  console.log("Продукт:", product);
}

function logTotalPrice(product) {
  const total = product.price * product.quantity;
  console.log(
    \`Загальна вартість товару "\${product.name}": \${product.quantity} × \${product.price} = \${total}\`
  );
}

// ===== Приклади виклику =====

createProduct(
  { name: "Ноутбук", price: 25000, quantity: 2 },
  logProduct
);

createProduct(
  { name: "Мишка", price: 800, quantity: 3 },
  logTotalPrice
);`)}

  <h3>Результат (скріншот Console)</h3>
  <img src="lab5/1z.png" alt="Console завдання 1.1">
`,


  task1_2: `
  <h2>Завдання 1.2 — обробка об’єкта medicines (стрілочні функції)</h2>

  <h3>Умова</h3>
  <pre>
З об'єкту medicines потрібно отримати масив   в якому будуть лише назви препаратів.

З масиву  потрібно прибрати медикаменти , в яких строк зберігання  уже пройшов . 
У новому масиві відсортувати медикаменти  у хронологічному порядку.
  Результат вивести у консоль.
Застосувати стрілочні функції

const medicines = {
    Агалгін: new Date("2022-05-01"),
    Ношпа: new Date("2025-07-02"),
    Альфахолін: new Date("2024-12-21"),
    Аспірин: new Date("2022-08-15"),
    Аспаркам: new Date("2024-04-18"),
   };

  </pre>

  <h3>Фрагмент коду</h3>
  ${codeBlock(`const medicines = {
  Агалгін: new Date("2022-05-01"),
  Ношпа: new Date("2026-09-02"),
  Альфахолін: new Date("2024-12-21"),
  Аспірин: new Date("2026-08-15"),
  Аспаркам: new Date("2024-04-18"),
};

const now = new Date();

const names = Object.keys(medicines);
console.log("Усі препарати:", names);

const validMedicines = names.filter(name => medicines[name] > now);
console.log("Дійсні препарати:", validMedicines);

const sortedMedicines = validMedicines.sort(
  (a, b) => medicines[a] - medicines[b]
);

console.log("Відсортовані по даті:", sortedMedicines);
`)}

  <h3>Результат (скріншот Console)</h3>
  <img src="lab5/2z.png" alt="Console завдання 1.2">
`,

  task1_3: `
  <h2>Завдання 1.3 — Масив фруктів: знижка 20% та додавання ID</h2>

  <h3>Умова</h3>
  <pre>
  Напишіть функцію, яка приймає массив об'єктів і повертає новий массив
  Зробіть знижку 20 % на всі фрукти у масиві
  Надайте ід для кожного продукту

  const fruits = [
    { name: "apple", price: 200 },
    { name: "orange", price: 300 },
    { name: "grapes", price: 750 },
  ];
  </pre>

  <h3>Фрагмент коду</h3>
  ${codeBlock(`const fruits = [
  { name: "apple", price: 200 },
  { name: "orange", price: 300 },
  { name: "grapes", price: 750 },
];

const transformFruits = (arr) =>
  arr.map((fruit, index) => ({
    ...fruit,
    id: index + 1,           // додаємо id
    price: fruit.price * 0.8 // робимо знижку 20%
  }));

// Виклик функції
const updatedFruits = transformFruits(fruits);

console.log("Оригінальний масив:", fruits);
console.log("Масив зі знижкою і ID:", updatedFruits);`)}

  <h3>Результат (скріншот Console)</h3>
  <img src="lab5/3z.png" alt="Console завдання 1.3">
`,

  task1_4: `
    <h2>Завдання 1.4 — Клас <code>Client</code> з приватними властивостями</h2>

    <h3>Умова</h3>
    <p>
      Напиши клас Client який створює об'єкт
      з властивостями login email
      Оголоси приватні властивості #login #email,
      доступ до яких зроби через геттер та сеттер login email
    </p>

    <h3>Код класу</h3>
    ${codeBlock(`class Client {
  #login;
  #email;

  constructor(login, email) {
    this.#login = login;
    this.#email = email;
  }

  // getter login
  get login() {
    return this.#login;
  }

  // setter login
  set login(newLogin) {
    this.#login = newLogin;
  }

  // getter email
  get email() {
    return this.#email;
  }

  // setter email
  set email(newEmail) {
    this.#email = newEmail;
  }
}`)}

    <h3>Перевірка роботи класу</h3>
    ${codeBlock(`const client1 = new Client("m_kutaev", "maksim@example.com");

console.log(client1.login);  // m_kutaev
console.log(client1.email);  // maksim@example.com

client1.login = "max_k";
client1.email = "max@gmail.com";

console.log(client1.login);  // max_k
console.log(client1.email);  // max@gmail.com`)}

    <h3>Пояснення</h3>
    <ul>
      <li><code>#login</code> та <code>#email</code> — приватні поля, до них не можна звернутися напряму поза класом.</li>
      <li>Геттери <code>login</code> / <code>email</code> повертають поточні значення приватних полів.</li>
      <li>Сеттери <code>login</code> / <code>email</code> дозволяють оновлювати значення цих полів.</li>
    </ul>
    <h3>Результат (скріншот Console)</h3>
    <img src="lab5/4z.png" alt="Console завдання 1.4">
  `,

  task1_5: `
  <h2>Завдання 2.3 — Підрахунок кількості тегів у масиві твітів</h2>

  <h3>Умова</h3>
  <pre>
Повернути об'єкт, у якому вказано кількість кожного тегу.
Очікуваний результат:
{ js: 3, nodejs: 3, html: 2, css: 2, react: 2 }

Вхідні дані:
const tweets = [
  { id: "000", likes: 5, tags: ["js", "nodejs"] },
  { id: "001", likes: 2, tags: ["html", "css"] },
  { id: "002", likes: 17, tags: ["html", "js", "nodejs"] },
  { id: "003", likes: 8, tags: ["css", "react"] },
  { id: "004", likes: 0, tags: ["js", "nodejs", "react"] },
];
  </pre>

  <h3>Фрагмент коду</h3>
  ${codeBlock(`const tweets = [
  { id: "000", likes: 5, tags: ["js", "nodejs"] },
  { id: "001", likes: 2, tags: ["html", "css"] },
  { id: "002", likes: 17, tags: ["html", "js", "nodejs"] },
  { id: "003", likes: 8, tags: ["css", "react"] },
  { id: "004", likes: 0, tags: ["js", "nodejs", "react"] },
];

// Функція підрахунку тегів
const countTags = (arr) =>
  arr
    .flatMap(tweet => tweet.tags)
    .reduce((acc, tag) => {
      acc[tag] = (acc[tag] || 0) + 1;
      return acc;
    }, {});

// Виклик
console.log(countTags(tweets));`)}

  <h3>Очікуваний результат (Console)</h3>
  <pre>
{ 
  js: 3,
  nodejs: 3,
  html: 2,
  css: 2,
  react: 2
}
  </pre>

  <h3>Скріншот Console</h3>
  <img src="lab5/5z.png" alt="Console Завдання 2.3">
`,

task1_6: `
    <h2>Завдання 1.6 — Перевірка правильності дужок</h2>

    <p>
      Напишіть функцію checkBrackets(str) яка приймає рядок жс коду (someFn)  і перевіряє правильність закриття дужок () {} []
      Якщо рядок містить коректний код функція повертає true.
      В іншому випадку повертає false
    </p>

    <p>
      Функція повинна повернути <code>true</code>, якщо всі дужки відкриті й закриті
      коректно і в правильному порядку, і <code>false</code> — якщо є помилка.
      Для розв’язання використовується стек.
    </p>

    <h3>Код функції <code>checkBrackets</code></h3>
    ${codeBlock(`function checkBrackets(str) {
  const stack = [];
  const pairs = {
    ")": "(",
    "}": "{",
    "]": "["
  };

  for (let char of str) {
    // відкривальні дужки кладемо у стек
    if (["(", "{", "["].includes(char)) {
      stack.push(char);
    }
    // для закривальних — перевіряємо відповідність
    else if ([")", "}", "]"].includes(char)) {
      const last = stack.pop();
      if (last !== pairs[char]) {
        return false; // неправильна пара
      }
    }
  }

  // якщо стек порожній — усі дужки закриті правильно
  return stack.length === 0;
}`)}

    <h3>Приклади виклику та результати</h3>
    ${codeBlock(`console.log(checkBrackets("function test() { return [1, 2, 3]; }"));
// true

console.log(checkBrackets("if (a > b { console.log(a); }"));
// false

console.log(checkBrackets("([{}])"));
// true

console.log(checkBrackets("([)]"));
// false`)}

    <p>
      У прикладах видно, що для коректно вкладених дужок функція повертає
      <code>true</code>, а для неправильних поєднань (наприклад, <code>"([)]"</code>)
      — <code>false</code>. Такий підхід зручний для базової перевірки синтаксису
      дужок у JS-коді.
    </p>

    <h3>Скріншот Console</h3>
  <img src="lab5/6z.png" alt="Console Завдання 2.3">
  `,

  // Завдання 2
task2_1: `
  <h2>Завдання 2.1 — Отримати один масив значень із вкладених масивів</h2>

  <h3>Умова</h3>
  <pre>
Дано масив об'єктів. Необхідно створити новий масив, який містить 
усі значення з масивів values кожного об'єкта, зібрані в один масив.

Очікуваний результат:
[1, 2, 3, 4, 5, 6, 7, 8, 9]
  </pre>

  <h3>Вхідні дані</h3>
  ${codeBlock(`const data = [
  { id: 1, values: [1, 2, 3] },
  { id: 2, values: [4, 5, 6] },
  { id: 3, values: [7, 8, 9] },
];`)}

  <h3>Код розв'язання</h3>
  ${codeBlock(`const result = data.flatMap(item => item.values);

console.log(result);`)}

  <h3>Пояснення</h3>
  <p>
    Використано метод <code>flatMap()</code>, який послідовно:
  </p>
  <ul>
    <li>проходить по кожному елементу масиву (<code>map</code>)</li>
    <li>дістає значення поля <code>values</code></li>
    <li>"розгладжує" вкладений масив в один рівень (<code>flat</code>)</li>
  </ul>

  <h3>Результат</h3>
  ${codeBlock(`[1, 2, 3, 4, 5, 6, 7, 8, 9]`)}

  <p>
    Таким чином, усі значення з вкладених масивів об'єднані в один суцільний масив.
  </p>
  <h3>Скріншот Console</h3>
  <img src="lab5/2_1.png" alt="Console Завдання 2.3">
`,

task2_2: `
  <h2>Завдання 2.2 — Перевірити, чи всі елементи масиву парні</h2>

  <h3>Умова</h3>
  <pre>
Дано масив чисел: [2, 4, 6, 8, 10].
Необхідно перевірити, чи є кожен елемент масиву парним.

Очікуваний результат:
true
  </pre>

  <h3>Вхідні дані</h3>
  ${codeBlock(`const numbers = [2, 4, 6, 8, 10];`)}

  <h3>Код розв'язання</h3>
  ${codeBlock(`const allEven = numbers.every(num => num % 2 === 0);

console.log(allEven); // true`)}

  <h3>Пояснення</h3>
  <p>
    Використано метод <code>every()</code>, який перевіряє,
    чи всі елементи масиву задовольняють умову. 
    Умова для кожного елемента:
  </p>

  ${codeBlock(`num % 2 === 0  // число є парним`)}

  <p>
    Оскільки всі числа в масиві парні, результат буде:
    <code>true</code>.
  </p>

  <h3>Результат</h3>
  ${codeBlock(`true`)}

    <h3>Скріншот Console</h3>
  <img src="lab5/2_2.png" alt="Console Завдання 2.3">
`,


task2_3: `
  <h2>Завдання 2.3 — Сортування масиву рядків за алфавітом</h2>

  <h3>Умова</h3>
  <pre>
Дано масив рядків:
["banana", "orange", "apple", "pear"]

Потрібно відсортувати його у алфавітному порядку.

Очікуваний результат:
["apple", "banana", "orange", "pear"]
  </pre>

  <h3>Вхідні дані</h3>
  ${codeBlock(`const stringArray = ["banana", "orange", "apple", "pear"];`)}

  <h3>Код розв'язання</h3>
  ${codeBlock(`const sorted = stringArray.slice().sort();

console.log(sorted);
// ["apple", "banana", "orange", "pear"]`)}

  <h3>Пояснення</h3>
  <p>
    Для розв'язання використано метод <code>sort()</code>, який
    сортує рядки у алфавітному порядку.
    Метод <code>slice()</code> застосовано для створення копії масиву,
    щоб не змінювати оригінальний масив <code>stringArray</code>.
  </p>

  <h3>Результат</h3>
  ${codeBlock(`["apple", "banana", "orange", "pear"]`)}

  <h3>Скріншот Console</h3>
  <img src="lab5/2_3.png" alt="Console Завдання 2.3">
`,

task2_4: `
  <h2>Завдання 2.4 — Клас Calculator з підтримкою method chaining</h2>

  <h3>Умова</h3>
  <pre>
Розробити клас Calculator, який дозволяє виконувати арифметичні
операції над числом за допомогою методів класу, підтримуючи
ланцюжкові виклики (method chaining).

Вимоги:
- number(value) — встановлює початкове значення.
- getResult() — повертає поточний результат.
- add(value) — додає value.
- subtract(value) — віднімає value.
- divide(value) — ділить на value, при value = 0 → помилка.
- multiply(value) — множить на value.
- Усі методи, крім getResult(), повертають this.
  </pre>

  <h3>Код класу Calculator</h3>
  ${codeBlock(`class Calculator {
  constructor() {
    this.value = 0;
  }

  // Встановити початкове значення
  number(value) {
    this.value = value;
    return this;
  }

  // Повернути результат
  getResult() {
    return this.value;
  }

  // Додавання
  add(value) {
    this.value += value;
    return this;
  }

  // Віднімання
  subtract(value) {
    this.value -= value;
    return this;
  }

  // Ділення
  divide(value) {
    if (value === 0) {
      throw new Error("Неможливо виконати ділення на 0!");
    }
    this.value /= value;
    return this;
  }

  // Множення
  multiply(value) {
    this.value *= value;
    return this;
  }
}`)}


  <h3>Приклад використання</h3>
  ${codeBlock(`const calc = new Calculator();

const result = calc
  .number(10)   // 10
  .add(5)       // 15
  .subtract(3)  // 12
  .multiply(4)  // 48
  .divide(2)    // 24
  .getResult(); // повертає 24

console.log(result); // 24`)}

  <h3>Пояснення</h3>
  <p>
    Кожен метод класу (add, subtract, multiply, divide, number)
    повертає <code>this</code>, що дозволяє будувати ланцюжки викликів.
    Метод <code>getResult()</code> просто повертає поточне значення, 
    не змінюючи його. У методі <code>divide()</code> реалізована
    перевірка на ділення на нуль.
  </p>

  <h3>Результат (Console)</h3>
  <img src="lab5/2_4.png" alt="Console Завдання 2.4">
`,


final5: `
  <h2>Висновки</h2>

  <p>
    У ході виконання лабораторної роботи №5 було закріплено та розширено знання 
    щодо роботи з об'єктами та масивами об'єктів у JavaScript, а також засвоєно 
    важливі концепції сучасного програмування — callback-функції, стрілочні функції 
    та перебираючі методи масиву.
  </p>

  <p>
    Під час виконання індивідуальних завдань було реалізовано:
  </p>

  <ul>
    <li>створення та модифікація об’єктів, доступ до їх властивостей і методів;</li>
    <li>використання приватних полів класів;</li>
    <li>формування нових масивів на основі існуючих даних;</li>
    <li>використання callback-функцій для обробки даних;</li>
    <li>застосування стрілочних функцій у різних контекстах;</li>
    <li>робота з методами <code>map</code>, <code>filter</code>, <code>reduce</code>, 
        <code>every</code>, <code>flatMap</code>, <code>sort</code>;</li>
    <li>створення власних класів із підтримкою ланцюжкових викликів (method chaining).</li>
  </ul>

  <p>
    Виконання роботи дозволило краще зрозуміти підходи до структуризації даних,
    побудови більш гнучких і масштабованих рішень, а також ознайомило 
    з особливостями функціонального стилю програмування в JavaScript.
  </p>

  <p>
    Отже, поставленої мети лабораторної роботи досягнуто: отримані практичні 
    навички роботи з об'єктами, масивами об'єктів, методами їх обробки, callback-функціями, 
    стрілочними функціями та перебираючими методами масивів.
  </p>
`,

};

const lab6 = {
  theme6: `
  <h2>Лабораторна робота №6</h2>
  <h3>Тема:</h3>

  <p>
    Прототипи та класи в JavaScript. Робота з DOM-деревом. 
    Обробка подій, об’єкт події та делегування подій.
  </p>

  <h3>Мета роботи:</h3>
  <p>
    Закріпити на практиці принципи об’єктно-орієнтованого програмування 
    в JavaScript, навчитися створювати класи, працювати з прототипами 
    та наслідуванням. Ознайомитися з можливостями DOM-API, 
    навчитися змінювати структуру HTML-документа засобами JavaScript, 
    опрацьовувати події, використовувати об’єкт події та застосовувати 
    делегування подій для оптимізації роботи інтерфейсу.
  </p>

  <h3>Місце розташування звіту:</h3>
  <div class="link-buttons">
    <a class="btn" href="https://github.com/MaixmK/IK-33_appWEB-Kutaiev-Maksym-FIOT-2025" target="_blank" rel="noopener">Репозиторій застосунку</a>
    <a class="btn" href="https://maixmk.github.io/IK-33_appWEB-Kutaiev-Maksym-FIOT-2025/" target="_blank" rel="noopener">Жива сторінка застосунку</a>
    <a class="btn" href="https://github.com/MaixmK/IK-33_appRECORD-Kutaiev-Maksym-FIOT-2025" target="_blank" rel="noopener">Репозиторій звітів</a>
    <a class="btn" href="https://maixmk.github.io/IK-33_appRECORD-Kutaiev-Maksym-FIOT-2025/" target="_blank" rel="noopener">Жива сторінка звітв</a>
  </div>
  <h3>Місце розташування виконаних програм:</h3>
  <p>Усі виконані скрипти підключено до відповідних розділів лабораторної роботи та доступні у вихідному коді звіту.</p>
`,

  task1: `
  <h2>Завдання №1</h2>

  <p>
    У звітному HTML-документі створити розмітку, що складається з тексту,
    кнопки та поля введення. Натискання на кнопку <b>SHOW ME</b>
    має виводити значення з поля введення у консолі.
  </p>

  <h3>Робота програми</h3>
  <div class="task1-demo" style="background:#8cc689;padding:15px;display:inline-block;">
    <div style="font-weight:bold;font-size:24px;margin-bottom:10px;">ЗАВДАННЯ №1</div>
    <button id="showMeBtn">SHOW ME</button>
    <input id="showMeInput" type="text" placeholder="Введіть будь-який текст" />
  </div>

  <p style="margin-top:15px;">
    При натисканні на кнопку текст із поля введення з’являється у <b>Console</b> браузера.
  </p>

  <h3>Фрагмент програмного коду</h3>
  ${codeBlock(`// HTML-розмітка
<div class="task1-demo">
  <h2>ЗАВДАННЯ №1</h2>
  <button id="showMeBtn">SHOW ME</button>
  <input id="showMeInput" type="text" placeholder="Введіть будь-який текст" />
</div>

// JavaScript-обробник події
document.addEventListener('click', (event) => {
  if (event.target.id === 'showMeBtn') {
    const input = document.getElementById('showMeInput');
    console.log('Введене значення:', input.value);
  }
});`)}
`,
task2: `
  <h2>ЗАВДАННЯ 2</h2>

  <p>
    У звітному HTML-документі створити html-розмітку, що складається з тексту,
    кнопки та поля введення. Кнопка <b>«Приховати»</b> ховає введений текст,
    замінюючи його на зірочки. Кнопка змінює свій текст на <b>«Розкрити»</b>.
    При повторному натисканні текст знову стає доступним, а кнопка повертається
    до попереднього стану.
  </p>

  <div class="task3-demo" style="background:#8cc689;padding:15px;display:inline-block;">
    <div style="font-weight:bold;font-size:24px;margin-bottom:10px;">ЗАВДАННЯ 3</div>

    <input
      id="passwordInput"
      type="text"
      placeholder="пароль"
      style="padding:4px 6px;border:1px solid #999;outline:none;"
    />
    <button id="passwordToggleBtn">Приховати</button>
  </div>

  <h3>Фрагмент програмного коду</h3>
  ${codeBlock(`// HTML-розмітка
<div class="task3-demo">
  <h2>ЗАВДАННЯ 3</h2>
  <input id="passwordInput" type="text" placeholder="пароль" />
  <button id="passwordToggleBtn">Приховати</button>
</div>

// JavaScript — приховати / розкрити введений текст
document.addEventListener('click', (event) => {
  if (event.target.id === 'passwordToggleBtn') {
    const input = document.getElementById('passwordInput');
    if (!input) return;

    if (input.type === 'text') {
      input.type = 'password';
      event.target.textContent = 'Розкрити';
    } else {
      input.type = 'text';
      event.target.textContent = 'Приховати';
    }
  }
});`)}
`,
task3: `
  <h2>ЗАВДАННЯ 5</h2>

  <p>
    У звітному HTML-документі створено розмітку з використанням
    селектора класу <code>class="taskTitle"</code> для тексту та
    селектора ідентифікатора <code>id="place"</code> для зеленого блоку.
    На об’єкт <code>window</code> додано слухач події <code>click</code>,
    який визначає, чи користувач клікнув у зелений прямокутник.
  </p>

  <div style="background:#8cc689;padding:15px;display:inline-block;">
    <div class="taskTitle" style="font-weight:bold;font-size:24px;margin-bottom:10px;">
      ЗАВДАННЯ 5
    </div>

    <div
      id="place"
      style="
        width:120px;
        height:260px;
        background:green;
        margin-top:10px;
      "
    ></div>
  </div>

  <p style="margin-top:10px;">
    Якщо користувач клацає по зеленому прямокутнику або його вмісту —
    у консолі виводиться <b>true</b>. Якщо клацнуто в будь-якому іншому
    місці вікна — виводиться <b>false</b>.
  </p>

  <h3>Фрагмент програмного коду</h3>
  ${codeBlock(`<!-- HTML -->
<div class="taskTitle">ЗАВДАННЯ 5</div>
<div id="place"></div>

<!-- JavaScript -->
window.addEventListener('click', function (event) {
  var place = document.getElementById('place');
  if (!place) return;

  // Перевіряємо, чи клік був всередині блоку з id="place"
  var clickedInside = place.contains(event.target);

  console.log(clickedInside);
});`)}
`,
task4: `
  <h2>ЗАВДАННЯ 7</h2>

  <p>
    У звітному HTML-документі міститься список категорій
    <code>&lt;ul id="categories"&gt;</code>. Необхідно за допомогою JavaScript:
  </p>

  <ol>
    <li>Порахувати і вивести в консоль кількість категорій (елементів <code>li.item</code>).</li>
    <li>Для кожного елемента <code>li.item</code> вивести заголовок категорії (тег <code>&lt;h2&gt;</code>) та кількість вкладених елементів <code>&lt;li&gt;</code>.</li>
  </ol>

  <p>Для виконання потрібно використати метод <b>forEach()</b> та властивості навігації DOM.</p>

  <div style="background:#8cc689;padding:15px;display:inline-block;">
    <div style="font-weight:bold;font-size:24px;margin-bottom:10px;">ЗАВДАННЯ 7</div>

    <ul id="categories" style="padding-left:20px;">
      <li class="item">
        <h2>Animals</h2>
        <ul>
          <li>Cat</li>
          <li>Hamster</li>
          <li>Horse</li>
          <li>Parrot</li>
        </ul>
      </li>

      <li class="item">
        <h2>Products</h2>
        <ul>
          <li>Bread</li>
          <li>Prasley</li>
          <li>Cheese</li>
        </ul>
      </li>

      <li class="item">
        <h2>Technologies</h2>
        <ul>
          <li>HTML</li>
          <li>CSS</li>
          <li>JavaScript</li>
          <li>React</li>
          <li>Node.js</li>
        </ul>
      </li>
    </ul>
  </div>

  <h3>Фрагмент програмного коду</h3>

  ${codeBlock(`// Знаходимо список категорій
const categoriesList = document.querySelector('#categories');

// 1. Кількість категорій
const categoryItems = categoriesList.querySelectorAll('.item');
console.log('Number of categories:', categoryItems.length);

// 2. Для кожного елемента li.item вивести назву категорії і кількість елементів
categoryItems.forEach(item => {
  const title = item.querySelector('h2').textContent;         // Заголовок категорії
  const elementsCount = item.querySelectorAll('ul li').length; // Кількість вкладених li

  console.log('Category:', title);
  console.log('Elements:', elementsCount);
});`)}

  <p style="margin-top:10px;">
    У консолі браузера будуть виведені дані в такому форматі:
    <br/><br/>
    Number of categories: 3<br/>
    Category: Animals — Elements: 4<br/>
    Category: Products — Elements: 3<br/>
    Category: Technologies — Elements: 5
  </p>
`,

task5: `
  <h2>ЗАВДАННЯ 5</h2>

  <p>
    Потрібно реалізувати скрипт керування формою логіна. Обробка має
    виконуватися за подією <code>submit</code>, без перезавантаження сторінки,
    з перевіркою заповнення полів і виведенням даних у консоль.
  </p>

  <div style="background:#8cc689;padding:15px;display:inline-block;">
    <div style="font-weight:bold;font-size:24px;margin-bottom:10px;">ЗАВДАННЯ 5</div>

    <form class="login-form">
      <label>
        Email
        <input type="email" name="email" />
      </label>
      <br />
      <label>
        Password
        <input type="password" name="password" />
      </label>
      <br />
      <button type="submit">Log in</button>
    </form>
  </div>

  <h3>Фрагмент програмного коду</h3>

  ${codeBlock(`// Завдання 5 — Визначення кліку всередині блоку з id="place"
window.addEventListener('click', function (event) {
  const place = document.getElementById('place');
  if (!place) return;

  const clickedInside = place.contains(event.target);
  console.log(clickedInside);
});


document.addEventListener('submit', (event) => {
  // Перевіряємо, що сабміт саме форми логіна
  const form = event.target;
  if (!form.classList.contains('login-form')) return;

  // 2. Забороняємо перезавантаження сторінки
  event.preventDefault();

  // 4. Дістаємо елементи форми через form.elements
  const { email, password } = form.elements;

  const emailValue = email.value.trim();
  const passwordValue = password.value.trim();

  // 3. Перевірка заповненості полів
  if (!emailValue || !passwordValue) {
    alert('All form fields must be filled in');
    return;
  }

  // 4. Формуємо об'єкт з даними
  const formData = {
    email: emailValue,
    password: passwordValue,
  };

  // 5. Виводимо в консоль і очищуємо форму
  console.log(formData);
  form.reset();
});
`)}

  <p style="margin-top:10px;">
    При сабміті форми:
    <br />— якщо якесь поле порожнє, зʼявляється <b>alert</b> із текстом
    <code>'All form fields must be filled in'</code>;
    <br />— якщо обидва поля заповнені, в <b>Console</b> виводиться об’єкт
    з даними <code>{ email, password }</code>, а форма очищається методом
    <code>reset()</code>.
  </p>
`,

task6: `
  <h2>ЗАВДАННЯ 6</h2>

  <p>
    Потрібно реалізувати скрипт, який змінює колір фону документа через
    інлайн-стиль <code>&lt;body&gt;</code> під час натискання кнопки
    <code>.change-color</code>, а також записує обраний колір у
    <code>&lt;span class="color"&gt;</code>.
  </p>

  <div style="background:#8cc689;padding:15px;display:inline-block;">
    <div style="font-weight:bold;font-size:24px;margin-bottom:10px;">ЗАВДАННЯ 6</div>

    <div class="widget">
      <p>Background color: <span class="color">-</span></p>
      <button type="button" class="change-color">Change color</button>
    </div>
  </div>

  <h3>Фрагмент програмного коду</h3>

  ${codeBlock(`// Функція для генерації випадкового hex-кольору
function getRandomHexColor() {
  return \`#\${Math.floor(Math.random() * 16777215)
    .toString(16)
    .padStart(6, 0)}\`;
}

// Делегування події — працює навіть у динамічному DOM
document.addEventListener('click', (event) => {
  if (event.target.classList.contains('change-color')) {
    const newColor = getRandomHexColor();

    // змінюємо фон body
    document.body.style.backgroundColor = newColor;

    // записуємо новий колір у span.color
    const span = document.querySelector('.color');
    if (span) span.textContent = newColor;
  }
});`)}
`,
task7: `
  <h2>ЗАВДАННЯ 7</h2>

  <p>
    Створити колекцію кольорових div-елементів за введеною кількістю. 
    Кнопка <b>Create</b> генерує нову колекцію, а <b>Destroy</b> очищає її.
  </p>

  <div id="controls" style="margin-bottom: 10px;">
    <input type="number" min="1" max="100" step="1" />
    <button type="button" data-create>Create</button>
    <button type="button" data-destroy>Destroy</button>
  </div>

  <div id="boxes" style="display:flex; flex-wrap:wrap; gap:6px;"></div>

  <h3>Фрагмент програмного коду</h3>

  ${codeBlock(`function getRandomHexColor() {
  return \`#\${Math.floor(Math.random() * 16777215)
    .toString(16)
    .padStart(6, 0)}\`;
}

function createBoxes(amount) {
  const boxesContainer = document.getElementById('boxes');
  if (!boxesContainer) return;

  boxesContainer.innerHTML = '';
  const boxes = [];
  let size = 30;

  for (let i = 0; i < amount; i++) {
    const box = document.createElement('div');
    box.style.width = \`\${size}px\`;
    box.style.height = \`\${size}px\`;
    box.style.backgroundColor = getRandomHexColor();
    boxes.push(box);
    size += 10;
  }

  boxesContainer.append(...boxes);
}

function destroyBoxes() {
  const boxesContainer = document.getElementById('boxes');
  if (!boxesContainer) return;
  boxesContainer.innerHTML = '';
}

document.addEventListener('click', (event) => {
  if (event.target.matches('#controls [data-create]')) {
    const controls = document.getElementById('controls');
    const input = controls.querySelector('input[type="number"]');
    const value = Number(input.value.trim());

    if (value < 1 || value > 100) {
      alert('Value must be between 1 and 100');
      return;
    }

    createBoxes(value);
    input.value = '';
  }

  if (event.target.matches('#controls [data-destroy]')) {
    destroyBoxes();
  }
});`)}
`,

task8: `
  <h2>Завдання 3. Делегування подій у каталозі WEB-застосунку</h2>

  <p>
    У власному WEB-застосунку реалізовано механізм <b>делегування подій</b> 
    для роботи з колекцією елементів каталогу. Картки товарів генеруються 
    динамічно, тому обробник подій встановлюється не на кожну картку, 
    а на спільний контейнер <code>#grid</code>.
  </p>

  <p>
    При натисканні кнопки <b>«Детальніше»</b> обробник через делегування 
    визначає, по якій картці натиснули, і відкриває модальне вікно 
    з інформацією про товар.
  </p>

  <h3>Фрагмент програмного коду</h3>

  ${codeBlock(`
// Делегування подій у каталозі
el.onclick = (e) => {
  const btn = e.target.closest('button[data-action]');
  if (!btn) return;

  const id = btn.dataset.id;
  const action = btn.dataset.action;

  if (action === 'details') {
    openModal(id);
  }
};

// Модальне вікно
function openModal(id) {
  const p = window.PRODUCTS.find(x => x.id === id);
  if (!p) return;

  document.querySelector('#m-img').src = p.img;
  document.querySelector('#m-title').textContent = p.title;
  document.querySelector('#m-desc').textContent = p.desc;

  document.querySelector('#modal').showModal();
}
  `)}
  
  <p>
    Таким чином, вимога виконана: при виборі елемента каталогу за допомогою 
    делегування подій відкривається модальне вікно з описом товару.
  </p>

   <h3>Результат </h3>
  <img src="lab6/modal.png" alt="modal">
  <img src="lab6/modal2.png" alt="modal" width="800">

`,
conclusion6: `
  <h2>Висновок</h2>
  <p>
    У ході виконання лабораторної роботи було опрацьовано принципи роботи з
    DOM-деревом та подіями в JavaScript, а також застосування делегування подій
    у динамічних інтерфейсах. Створено інтерактивні елементи: роботу з формами,
    приховування тексту, зміну стилів, визначення області кліку, генерацію
    елементів і керування їх відображенням.
  </p>
  <p>
    Особливу увагу приділено делегуванню подій, що дозволяє ефективно працювати
    з великою кількістю створюваних елементів та оптимізує структуру коду.
    Реалізація модального вікна у WEB-застосунку демонструє практичне
    застосування цього підходу.
  </p>
  <p>
    У результаті роботи були закріплені навички маніпуляції DOM-елементами,
    роботи зі стилями, валідатором форм, створення інтерактивних UI-компонентів.
    Мету лабораторної роботи досягнуто.
  </p>
`,

  
};

const lab7 = {
  pagination: `
    <h2>Завдання. Реалізація пагінації в каталозі товарів</h2>
    <p class="taskTitle">
      У власному WEB-каталозі реалізовано пагінацію: товари показуються
      сторінками по 6 елементів, є кнопки переходу між сторінками (prev/next та номери сторінок).
    </p>

    <h3>Фрагмент JavaScript-коду пагінації</h3>
    <pre class="code">${codeBlock(`
const PER_PAGE = 6;
let currentPage = 1;

// ... функції renderPagination, render, обробник кліків по .page-btn ...
`)}</pre>

    <h3>Фрагмент HTML-розмітки</h3>
    <pre class="code">${codeBlock(`
<div id="grid" class="products-grid"></div>
<div id="pagination" class="pagination"></div>
`)}</pre>

    <h3>Результат роботи</h3>
    <p>На скриншоті видно, як каталог розбитий на сторінки, а під товарами виводяться кнопки пагінації.</p>
    <img src="img/lab7-pagination.png" alt="Пагінація в каталозі">
  `
};

const lab8 = {
  theme8: `
  <h2>Практична робота №8 — Тема, мета, розташування</h2>

  <p>
    <b>Тема лабораторної роботи:</b>
    Розробка клієнтської частини WEB-застосунку.
    Бізнес-логіка, робота з DOM, обробка подій,
    збереження стану застосунку у браузері.
  </p>

  <p>
    <b>Мета лабораторної роботи:</b>
    Закріпити практичні навички створення клієнтської частини
    веб-застосунку з використанням JavaScript.
    Реалізувати бізнес-логіку взаємодії користувача з інтерфейсом,
    обробку подій DOM, динамічне оновлення сторінки без перезавантаження,
    а також збереження стану застосунку за допомогою LocalStorage.
  </p>

  <p>
    <b>Тема розробки:</b>
    веб-застосунок <i>«Онлайн-каталог комп’ютерних комплектуючих»</i>
    з можливістю перегляду товарів, фільтрації, перегляду детальної
    інформації та формування кошика користувача.
  </p>

  <p>
    <b>Мета розробки:</b>
    створити інтерактивний веб-застосунок, який забезпечує зручний
    доступ до інформації про комп’ютерні комплектуючі, дозволяє
    користувачу переглядати каталог товарів, додавати позиції до кошика,
    змінювати їх кількість та отримувати підсумкову вартість замовлення
    без перезавантаження сторінки.
  </p>

  <ul>
    <li>аналіз предметної області та структури онлайн-каталогів;</li>
    <li>реалізація бізнес-логіки роботи з товарами та кошиком;</li>
    <li>обробка подій користувача (click, input, submit);</li>
    <li>динамічне оновлення DOM-елементів;</li>
    <li>збереження стану кошика між сесіями за допомогою LocalStorage;</li>
    <li>перевірка коректності роботи інтерфейсу та сценаріїв взаємодії.</li>
  </ul>

  <h3 class="muted">Розташування</h3>

  <div class="link-buttons">
    <a class="btn" href="https://github.com/MaixmK/IK-33_appWEB-Kutaiev-Maksym-FIOT-2025" target="_blank" rel="noopener">
      Репозиторій застосунку
    </a>
    <a class="btn" href="https://maixmk.github.io/IK-33_appWEB-Kutaiev-Maksym-FIOT-2025/" target="_blank" rel="noopener">
      Жива сторінка застосунку
    </a>
    <a class="btn" href="https://github.com/MaixmK/IK-33_appRECORD-Kutaiev-Maksym-FIOT-2025" target="_blank" rel="noopener">
      Репозиторій звітів
    </a>
    <a class="btn" href="https://maixmk.github.io/IK-33_appRECORD-Kutaiev-Maksym-FIOT-2025/" target="_blank" rel="noopener">
      Жива сторінка звітів
    </a>
  </div>
`,

  
  section1: `
    <h3>Розділ 1</h3>

    <h3>Актуальність теми</h3>
    <p>
      Актуальність теми полягає у необхідності розв’язання проблеми ефективної організації
      доступу користувачів до інформації про комп’ютерні комплектуючі в умовах стрімкого
      розвитку інформаційних технологій та електронної комерції. Сучасний ринок комп’ютерної
      техніки характеризується великим асортиментом товарів, що ускладнює процес вибору та
      порівняння продукції для кінцевого користувача. Використання веб-застосунків для
      онлайн-каталогів дозволяє автоматизувати процес пошуку, перегляду та відбору товарів,
      підвищити зручність користування та скоротити час прийняття рішень. Актуальність також
      обумовлена необхідністю створення адаптивних інтерфейсів, які забезпечують коректну
      роботу веб-застосунків на різних пристроях, що є важливим фактором у сучасних умовах
      використання мобільних технологій.
    </p>

    <h3>Бізнес-логіка</h3>
    <p>
      Бізнес-логіка веб-застосунку являє собою сукупність правил та алгоритмів, які визначають
      порядок взаємодії користувача з системою. Вона описує сценарії роботи з каталогом товарів,
      відкриття детальної інформації про окремий товар, додавання товарів до кошика, зміну їх
      кількості та обчислення загальної вартості замовлення. Бізнес-логіка також визначає умови,
      за яких певні елементи інтерфейсу стають активними або неактивними, наприклад, блокування
      кнопки оформлення замовлення у випадку порожнього кошика.
    </p>

    <h3>Функціональні вимоги</h3>
    <p>
      Функціональні вимоги до системи визначають перелік дій, які повинен виконувати веб-застосунок.
      До них належать можливість перегляду каталогу комп’ютерних комплектуючих, відкриття детальної
      інформації про товар у модальному вікні, додавання товарів до кошика, редагування кількості
      вибраних позицій, очищення кошика та відображення підсумкової вартості замовлення. Усі функції
      повинні виконуватися без перезавантаження сторінки, що забезпечує зручну та інтуїтивну взаємодію
      користувача з системою.
    </p>

    <h3>Нефункціональні вимоги</h3>
    <p>
      Нефункціональні вимоги описують якісні характеристики роботи веб-застосунку. До них належать
      адаптивність інтерфейсу для коректного відображення на різних типах пристроїв, швидкодія та
      стабільність роботи, зручність користування, зрозумілий інтерфейс, а також сумісність із сучасними
      веб-браузерами. Важливою нефункціональною вимогою є також підтримка збереження даних користувача
      у браузері, що дозволяє відновлювати стан кошика між сесіями.
    </p>

    <h3>Use-case діаграма</h3>
    <p>
      Use-case діаграма використовується для графічного представлення функціональних можливостей системи
      з точки зору користувача. Вона відображає основні сценарії взаємодії між користувачем та веб-застосунком,
      зокрема перегляд каталогу, перегляд детальної інформації про товар, додавання товарів до кошика та
      оформлення замовлення. Use-case діаграма дозволяє чітко визначити межі системи, ролі користувачів та
      основні функції, які надає веб-застосунок, що спрощує розуміння логіки його роботи навіть без детального
      розгляду технічної реалізації.
    </p>

    <p>Діаграма</p>
    <img src="img/uml.png"  width = 100%alt="Пагінація в каталозі">
  `,

section2: `
<h2>2 РОЗРОБЛЕННЯ АДАПТИВНОГО ІНТЕРФЕЙСУ WEB-ЗАСТОСУНКУ</h2>

<p>
У даному розділі описано процес розроблення адаптивного інтерфейсу
веб-застосунку для онлайн-каталогу комп’ютерних комплектуючих.
Розглянуто реалізацію основних структурних компонентів інтерфейсу,
застосовані стилістичні та функціональні рішення, а також організацію
файлової структури проєкту та особливості розміщення результатів роботи
у репозиторії GitHub.
</p>

<p>
Інтерфейс веб-застосунку спроєктовано з урахуванням принципів
адаптивності та зручності користування. Для коректного відображення
контенту на пристроях з різною роздільною здатністю застосовано
сучасні засоби CSS, зокрема Flexbox та Grid, а також медіа-запити.
</p>

<h3>2.1 Шапка (header) з логотипом та навігаційним меню</h3>

<p>
Шапка веб-застосунку виконує роль основного навігаційного елемента
та забезпечує швидкий доступ користувача до ключових розділів сайту.
У header розміщено логотип застосунку та панель дій з переходом
на головну сторінку, сторінку авторизації та кошик.
</p>

<p>
Для вирівнювання елементів та адаптивної поведінки меню
використано Flexbox. Лічильник товарів у кошику оновлюється
динамічно за допомогою JavaScript.
</p>

<h4>Фрагмент HTML-коду шапки</h4>
${codeBlock(`
<header class="site-header">
  <div class="topbar container">
    <a class="logo" href="index.html">Комплектуючі</a>
    <div class="actions">
      <a class="icon-btn" href="index.html">🏠</a>
      <a class="icon-btn" href="authorization/login.html">👤</a>
      <a class="icon-btn" href="cart.html">🛒</a>
      <span id="cartCount">0</span>
    </div>
  </div>
</header>
`)}

<h3>2.2 Основний блок з інформаційними картками</h3>

<p>
Основний блок веб-застосунку призначений для відображення
каталогу комп’ютерних комплектуючих та сторінки кошика.
Контент подається у вигляді інформаційних карток і таблиць,
що дозволяє користувачу зручно взаємодіяти з товарами.
</p>

<p>
Для реалізації адаптивної сітки використано Flexbox та Grid.
Кількість колонок змінюється залежно від ширини екрана:
на десктопах — декілька колонок, на мобільних пристроях — одна.
</p>

<h4>Фрагмент HTML-коду основного блоку (кошик)</h4>
${codeBlock(`
<main class="container cart-page">
  <h1 class="cart-title">Кошик</h1>

  <div class="cart-grid">
    <section class="cart-card">
      <div class="cart-head">
        <h2 class="cart-subtitle">Товари</h2>
        <button class="btn" id="btnClearCart" type="button">Очистити</button>
      </div>

      <div id="cartEmpty" class="cart-empty" hidden>
        Кошик порожній. Перейдіть у <a href="index.html#shop">каталог</a>.
      </div>

      <table class="cart-table" id="cartTable">
        <thead>
          <tr>
            <th>Товар</th>
            <th class="right">Ціна</th>
            <th class="center">К-сть</th>
            <th class="right">Сума</th>
            <th class="center">Дія</th>
          </tr>
        </thead>
        <tbody id="cartBody"></tbody>
      </table>
    </section>

    <aside class="cart-card">
      <h2 class="cart-subtitle">Підсумок</h2>
      <div class="cart-summary">
        <div class="row">Позицій: <span id="sumItems">0</span></div>
        <div class="row">Товарів: <span id="sumQty">0</span></div>
        <div class="row total">Разом: <span id="sumTotal">0 ₴</span></div>
        <button class="btn btn--primary" id="btnCheckout" disabled>
          Оформити
        </button>
      </div>
    </aside>
  </div>
</main>
`)}

<h3>2.3 Нижній колонтитул (footer)</h3>

<p>
Нижній колонтитул веб-застосунку містить довідкову інформацію
та додаткові навігаційні посилання. Він завершує структуру сторінки
та не перевантажує інтерфейс зайвими елементами.
</p>

<p>
Для коректного вирівнювання елементів та адаптивності
використано Flexbox.
</p>

<h4>Фрагмент HTML-коду footer</h4>
${codeBlock(`
<footer class="site-footer">
  <div class="footer__grid container">
    <small>© <span id="year"></span> Комплектуючі</small>
    <ul class="footer__links">
      <li><a href="about.html">Про проєкт</a></li>
    </ul>
  </div>
</footer>
`)}
`,

functionalStyling: `
<h2>Функціонально-стилістичні вимоги</h2>

<p>
Функціонально-стилістичні вимоги до веб-застосунку спрямовані на
забезпечення зручності користування, естетичної привабливості
інтерфейсу та коректної роботи на пристроях з різними характеристиками.
Стилізація елементів веб-застосунку виконана із застосуванням
каскадних таблиць стилів CSS.
</p>

<p>
Для оформлення кнопок, карток товарів, навігаційних елементів та
інформаційних блоків використано єдину кольорову гаму, узгоджені
шрифти та відступи, що забезпечує цілісність дизайну та покращує
сприйняття інтерфейсу користувачем.
</p>

<h3>Стилізація елементів веб-застосунку</h3>

<p>
Основні елементи інтерфейсу (кнопки, картки, таблиці, навігація)
оформлені з використанням CSS-класів. Для інтерактивних елементів
застосовано hover-ефекти та transition-анімації.
</p>

<h4>Приклад стилізації кнопок</h4>
${codeBlock(`
.btn {
  padding: 10px 16px;
  border-radius: 6px;
  border: none;
  background-color: #4CAF50;
  color: #fff;
  cursor: pointer;
  transition: background-color 0.3s ease, transform 0.2s ease;
}

.btn:hover {
  background-color: #43a047;
  transform: scale(1.03);
}
`)}

<h3>Адаптивна навігація</h3>

<p>
Навігаційна панель веб-застосунку реалізована з урахуванням адаптивності.
На пристроях з великою роздільною здатністю навігаційне меню
відображається у вигляді горизонтальної панелі, тоді як на екранах
з меншою шириною елементи можуть трансформуватися у компактне меню.
</p>

<p>
Для вирівнювання елементів навігації використано Flexbox.
</p>

<h4>Приклад CSS для навігації</h4>
${codeBlock(`
.topbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.actions {
  display: flex;
  gap: 12px;
}
`)}

<h3>Адаптивна сітка контенту</h3>

<p>
Адаптивна сітка контенту реалізована з використанням технологій
Flexbox та CSS Grid. Розташування карток автоматично перебудовується
залежно від ширини екрана, що забезпечує зручний перегляд
контенту на десктопах, планшетах і смартфонах.
</p>

<h4>Приклад реалізації сітки контенту</h4>
${codeBlock(`
.cart-grid {
  display: grid;
  grid-template-columns: 2fr 1fr;
  gap: 20px;
}

@media (max-width: 768px) {
  .cart-grid {
    grid-template-columns: 1fr;
  }
}
`)}

<h3>Використання медіа-запитів</h3>

<p>
Для адаптації стилів під різні розміри екранів та орієнтацію пристрою
використано медіа-запити (media queries). За їх допомогою змінюються
розміри шрифтів, відступи та розташування основних блоків інтерфейсу.
</p>

<h4>Приклад медіа-запиту</h4>
${codeBlock(`
@media (max-width: 480px) {
  .cart-title {
    font-size: 20px;
  }

  .btn {
    width: 100%;
  }
}
`)}

<h3>Візуальні ефекти та анімації</h3>

<p>
З метою підвищення зручності та наочності взаємодії користувача
з інтерфейсом у веб-застосунку реалізовано візуальні ефекти
та анімації. Застосовано плавні переходи при наведенні курсора
на кнопки та елементи керування, а також анімацію відкриття
інтерфейсних компонентів.
</p>

<h4>Приклад використання transition</h4>
${codeBlock(`
.icon-btn {
  transition: transform 0.2s ease;
}

.icon-btn:hover {
  transform: scale(1.1);
}
`)}

<h3>Файлова структура веб-застосунку</h3>

<p>
Файлова структура веб-застосунку організована логічно та відповідає
принципам модульності. HTML-документи, таблиці стилів, JavaScript-скрипти
та графічні ресурси зберігаються у відповідних каталогах, що спрощує
підтримку та масштабування проєкту.
</p>
`,
githubPlacement: `
<h2>Розміщення результатів роботи в GitHub</h2>

<p>
Результати виконання індивідуального проєкту було розміщено у відкритому
репозиторії на платформі GitHub. Для цього створено окремий репозиторій,
призначений для зберігання вихідного коду, стилів, JavaScript-скриптів
та допоміжних ресурсів веб-застосунку для онлайн-каталогу
комп’ютерних комплектуючих.
</p>

<p>
Використання системи контролю версій Git дозволило забезпечити
збереження історії змін, контроль якості коду та впорядкованість
процесу розроблення. Кожен етап роботи фіксувався у вигляді
логічно завершених комітів.
</p>

<h3>Файл README.md</h3>

<p>
У корені репозиторію розміщено файл <b>README.md</b>, який містить
короткий опис призначення веб-застосунку, перелік використаних
технологій, а також інструкцію щодо запуску проєкту.
</p>

<p>
У README.md наведено рекомендації щодо відкриття веб-застосунку
у браузері та запуску через локальний веб-сервер для коректної
роботи JavaScript та механізмів збереження даних у LocalStorage.
</p>

<h4>Приклад структури README.md</h4>
${codeBlock(`
# Онлайн-каталог комп’ютерних комплектуючих

Веб-застосунок для перегляду комп’ютерних комплектуючих,
детального ознайомлення з товарами та формування кошика.

## Використані технології
- HTML5
- CSS3 (Flexbox, Grid, Media Queries)
- JavaScript (DOM API, LocalStorage)

## Запуск проєкту
1. Клонувати репозиторій
2. Відкрити index.html у браузері
або запустити через локальний сервер
`)}

<h3>Організація комітів</h3>

<p>
Процес розроблення супроводжувався регулярним створенням комітів,
кожен з яких відповідає окремому етапу або завершеному фрагменту роботи.
Коміти мають змістовні повідомлення, що чітко відображають сутність
виконаних змін.
</p>

<p>
Серед основних типів комітів можна виділити створення базової структури
проєкту, реалізацію адаптивного інтерфейсу, додавання модальних вікон,
впровадження логіки кошика та оптимізацію стилів.
</p>


<p>
Під час роботи з репозиторієм уникалося надмірної фрагментації комітів,
а також використання надто загальних або неінформативних описів.
Кожен коміт відображає завершену логічну зміну у проєкті, що відповідає
принципам якісної роботи з системами контролю версій та сприяє
підвищенню читабельності історії розроблення.
</p>
`,

progress: `
<h2>ХІД ВИКОНАННЯ РОБОТИ</h2>

<p>
У даному розділі наведено послідовність виконання індивідуального проєкту
та описано основні етапи розроблення веб-застосунку з прикладами
реалізованого програмного коду.
</p>

<h3>Створення базової структури веб-застосунку</h3>

<p>
На початковому етапі було створено кореневу папку веб-застосунку
з логічно організованою файловою структурою. Для зберігання
HTML-документів, таблиць стилів, JavaScript-скриптів та ресурсів
використано окремі каталоги, що спрощує підтримку та масштабування проєкту.
</p>

<h4>Приклад файлової структури проєкту</h4>
${codeBlock(`
/css
  styles.css
/js
  app.js
  cart.js
/assets
  /img
index.html
cart.html
`)}

<h3>Реалізація структурних компонентів інтерфейсу</h3>

<h4>Шапка (header) з навігацією</h4>

<p>
Для реалізації шапки веб-застосунку використано HTML-розмітку
з логотипом та навігаційними елементами. Вирівнювання компонентів
здійснюється за допомогою технології Flexbox.
</p>

<h4>Фрагмент HTML-коду шапки</h4>
${codeBlock(`
<header class="site-header">
  <div class="topbar container">
    <a class="logo" href="index.html">Комплектуючі</a>

    <div class="actions">
      <a class="icon-btn" href="index.html">🏠</a>
      <a class="icon-btn" href="authorization/login.html">👤</a>
      <a class="icon-btn" href="cart.html">🛒</a>
      <span id="cartCount">0</span>
    </div>
  </div>
</header>
`)}

<h4>Фрагмент CSS для розміщення елементів header</h4>
${codeBlock(`
.topbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
}
`)}

<p><b>Реалізована вимога:</b> структурний компонент header, адаптивна навігація.</p>

<h3>Адаптивна сітка контенту (Flexbox / Grid)</h3>

<p>
Для відображення карток товарів у каталозі використано адаптивну сітку,
реалізовану засобами CSS Grid. Кількість колонок автоматично змінюється
залежно від ширини екрана.
</p>

<h4>Приклад CSS Grid для каталогу</h4>
${codeBlock(`
.products-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
  gap: 16px;
}
`)}

<p><b>Реалізована вимога:</b> адаптивна сітка контенту з динамічною перебудовою.</p>

<h3>Адаптація під різні розміри екрана (media queries)</h3>

<p>
Для коректного відображення інтерфейсу на планшетах та смартфонах
використано медіа-запити, які змінюють параметри сітки та відступи
між елементами.
</p>

<h4>Приклад media queries з проєкту</h4>
${codeBlock(`
@media (max-width: 768px) {
  .products-grid {
    grid-template-columns: 1fr;
  }

  .actions {
    gap: 8px;
  }
}
`)}

<p><b>Реалізована вимога:</b> адаптація стилів під різні розміри екранів.</p>

<h3>Реалізація бургер-меню</h3>

<p>
На пристроях з малою шириною екрана навігаційне меню трансформується
у бургер-меню, яке відкривається та закривається за допомогою JavaScript
та CSS-анімацій.
</p>

<h4>Фрагмент JavaScript-коду керування меню</h4>
${codeBlock(`
const burger = document.getElementById('burger');
const menu = document.getElementById('nav-menu');

burger.addEventListener('click', () => {
  menu.classList.toggle('open');
});
`)}

<h4>CSS-анімація відкриття меню</h4>
${codeBlock(`
#nav-menu {
  transition: transform 0.3s ease;
}

#nav-menu.open {
  transform: translateX(0);
}
`)}

<p><b>Реалізована вимога:</b> адаптивна навігація та анімація інтерфейсу.</p>

<h3>Реалізація кошика та збереження даних</h3>

<p>
Для збереження стану кошика між сесіями використано LocalStorage API.
Це дозволяє зберігати інформацію про додані товари навіть після
перезавантаження сторінки.
</p>

<h4>Фрагмент JavaScript-коду з cart.js</h4>
${codeBlock(`
function loadCart() {
  return JSON.parse(localStorage.getItem('pc_cart')) || { items: [] };
}

function saveCart(cart) {
  localStorage.setItem('pc_cart', JSON.stringify(cart));
}
`)}

<p><b>Реалізована вимога:</b> бізнес-логіка та збереження даних користувача.</p>

<h3>Візуальні ефекти та анімації</h3>

<p>
Для підвищення зручності користування застосовано CSS-анімації
та hover-ефекти для кнопок і елементів керування.
</p>

<h4>Приклад hover-ефекту кнопки</h4>
${codeBlock(`
.btn {
  transition: background-color 0.3s ease, transform 0.2s ease;
}

.btn:hover {
  background-color: #222;
  transform: scale(1.05);
}
`)}

<p><b>Реалізована вимога:</b> візуальні ефекти та анімації інтерфейсу.</p>
`,

logic3: `
<h2>3 ПРОГРАМНА ЛОГІКА (JAVASCRIPT)</h2>

<p>
У даному розділі описано функціональність веб-застосунку, реалізовану засобами JavaScript.
Основна логіка побудована на маніпуляціях DOM для динамічного оновлення інтерфейсу, обробці
подій (<b>click</b>, <b>input/change</b>, <b>load/DOMContentLoaded</b>, <b>resize</b>, <b>storage</b>),
роботі з <b>LocalStorage</b> для збереження даних користувача (кошик), а також реалізації
окремих інтерактивних модулів, зокрема модальних вікон і калькулятора.
</p>

<h3>3.1 Опис функціоналу</h3>

<h4>Динамічне оновлення контенту сторінки (DOM-маніпуляції)</h4>
<p>
Інтерфейс оновлюється без перезавантаження сторінки шляхом додавання/видалення елементів
та зміни їхнього стану через DOM. Прикладом є рендеринг карток товарів у каталозі
та таблиці кошика.
</p>

<h4>Приклад динамічного рендеру каталогу (створення карток і вставка в DOM)</h4>
${codeBlock(`
function renderGrid(list){
  const el = $('#grid');
  if (!el) return;

  el.innerHTML = '';
  if(!list.length) {
    el.innerHTML = '<div class="empty">Нічого не знайдено.</div>'; 
    return; 
  }

  list.forEach(p=>{
    const card = document.createElement('article');
    card.className='card';
    card.innerHTML = \`...\`;
    el.appendChild(card);
  });
}
`)}

<h4>Приклад рендеру кошика (генерація рядків таблиці)</h4>
${codeBlock(`
function renderCart() {
  const body = $("#cartBody");
  if (!body) { updateCartCount(); return; }

  const cart = loadCart();
  const items = Array.isArray(cart.items) ? cart.items : [];
  body.innerHTML = "";

  for (const it of items) {
    const tr = document.createElement("tr");
    tr.innerHTML = \`...\`;
    body.appendChild(tr);
  }
}
`)}

<h4>Інтерактивність інтерфейсу</h4>
<p>
Інтерактивність реалізовано через обробку натискань на кнопки, відкриття модальних вікон,
зміну станів елементів інтерфейсу та виконання дій користувача.
</p>

<h4>Приклад відкриття/закриття модального вікна калькулятора</h4>
${codeBlock(`
icon.addEventListener("click", () => {
  modal.classList.remove("hidden");
});

closeBtn.addEventListener("click", () => {
  modal.classList.add("hidden");
});

modal.addEventListener("click", (e) => {
  if (e.target === modal) modal.classList.add("hidden");
});
`)}

<h4>Збереження даних (LocalStorage)</h4>
<p>
Для збереження стану кошика між перезавантаженнями сторінки застосовано LocalStorage.
Дані серіалізуються у JSON та зберігаються за ключем <code>pc_cart</code>.
</p>

<h4>Завантаження/збереження кошика</h4>
${codeBlock(`
const CART_KEY = "pc_cart";

function loadCart() {
  try {
    return JSON.parse(localStorage.getItem(CART_KEY)) || { items: [], updatedAt: Date.now() };
  } catch {
    return { items: [], updatedAt: Date.now() };
  }
}

function saveCart(cart) {
  cart.updatedAt = Date.now();
  localStorage.setItem(CART_KEY, JSON.stringify(cart));
  updateCartCount();
}
`)}

<h4>Оновлення лічильника товарів у шапці (cartCount)</h4>
${codeBlock(`
function updateCartCount() {
  const el = document.getElementById('cartCount');
  if (!el) return;

  const cart = loadCart();
  const totalQty = cart.items.reduce((s, it) => s + (it.qty || 0), 0);
  el.textContent = String(totalQty);
}
`)}

<h4>Асинхронні операції</h4>
<p>
У проєкті застосовано подієвий підхід, характерний для асинхронної обробки:
оновлення інтерфейсу виконується після певних подій та змін даних у сховищі (LocalStorage).
Крім того, структура каталогу дозволяє (за потреби) підключити отримання даних через
<code>fetch()</code> із локальних JSON-файлів або API без зміни загальної архітектури,
оскільки рендеринг каталогу та фільтрація виконуються через окремі функції
<code>render()</code> / <code>renderGrid()</code>.
</p>
`,

events32: `
<h2>3.2 Обробка подій</h2>

<p>
Обробка подій реалізована відповідно до принципів <b>DOM-event handling</b>. Події
використовуються для керування меню, модальними вікнами, кошиком, пагінацією,
пошуком/сортуванням та синхронізацією між вкладками.
</p>

<h3>Події завантаження (DOMContentLoaded)</h3>
<p>
Використовуються для ініціалізації логіки після повного завантаження DOM (щоб елементи
вже існували у документі).
</p>

<h4>Приклад ініціалізації бургер-меню</h4>
${codeBlock(`
document.addEventListener('DOMContentLoaded', () => {
  const burger = document.querySelector('.burger');
  const nav = document.getElementById('nav');
  if (!burger || !nav) return;

  burger.addEventListener('click', () => { ... });
  autoToggleMenu();
  window.addEventListener('resize', autoToggleMenu);
});
`)}

<h4>Приклад ініціалізації кошика</h4>
${codeBlock(`
document.addEventListener("DOMContentLoaded", () => {
  updateCartCount();
  bindCartEvents();
  renderCart();
});
`)}

<h3>Події кліку (click)</h3>
<p>
Події кліку застосовуються для відкриття модальних вікон, додавання товарів у кошик,
керування кількістю, очищення та оформлення.
</p>

<h4>Делегування кліків для кнопок каталогу (“Детальніше”, “🛒”)</h4>
${codeBlock(`
el.onclick = (e)=>{
  const btn = e.target.closest('button[data-action]');
  if(!btn) return;

  const id = btn.dataset.id;
  const action = btn.dataset.action;
  const product = (window.PRODUCTS||[]).find(x=>x.id===id);
  if(!product) return;

  if(action === 'details') openModal(id);
  if(action === 'add') addToCart(product, 1);
};
`)}

<h4>Кліки у кошику (+/−/remove) через делегування</h4>
${codeBlock(`
body.addEventListener("click", (e) => {
  const btn = e.target.closest("button[data-action]");
  if (!btn) return;

  const action = btn.dataset.action;
  if (action === "remove") { ... }

  const wrap = btn.closest(".qty");
  const id = wrap?.dataset.id;
  if (!id) return;

  if (action === "inc") changeQty(id, +1);
  if (action === "dec") changeQty(id, -1);
});
`)}

<h3>Події введення (input, change)</h3>
<p>
Події введення застосовано для фільтрації та сортування каталогу в реальному часі.
</p>

<h4>Пошук (input) і сортування (change)</h4>
${codeBlock(`
$('#q').addEventListener('input', () => {
  currentPage = 1;
  render();
});
$('#sort').addEventListener('change', () => {
  currentPage = 1;
  render();
});
`)}

<h3>Події resize (адаптивна поведінка меню)</h3>
<p>
Для адаптивної логіки бургер-меню використано подію зміни розміру вікна.
</p>

${codeBlock(`
window.addEventListener('resize', autoToggleMenu);
`)}

<h3>Події storage (синхронізація між вкладками)</h3>
<p>
Для коректного оновлення кошика у разі зміни LocalStorage в іншій вкладці використано подію <code>storage</code>.
</p>

${codeBlock(`
window.addEventListener("storage", (e) => {
  if (e.key === CART_KEY) renderCart();
});
`)}

<h3>Структура бізнес-логіки</h3>
<p>
Бізнес-логіка застосунку реалізована як набір функцій, що описують правила роботи з кошиком та каталогом. Наприклад:
</p>
<ul>
  <li><b>addToCart</b> збільшує кількість існуючої позиції або додає нову;</li>
  <li>кількість товару обмежується діапазоном (<b>clampQty</b>);</li>
  <li>підсумки (кількість позицій, загальна кількість товарів, сума) обчислюються централізовано через <b>calcSummary</b>;</li>
  <li>відображення інтерфейсу залежить від стану кошика (порожній/непорожній), що реалізовано в <b>renderCart()</b>.</li>
</ul>

<h4>Приклад правила: обмеження кількості товару</h4>
${codeBlock(`
function clampQty(value) {
  const n = Number(value);
  if (!Number.isFinite(n)) return 1;
  return Math.max(1, Math.min(999, Math.floor(n)));
}
`)}

<h4>Приклад підрахунку підсумків</h4>
${codeBlock(`
function calcSummary(items) {
  const sumItems = items.length;
  const sumQty = items.reduce((s, it) => s + (it.qty || 0), 0);
  const sumTotal = items.reduce((s, it) => s + (it.price || 0) * (it.qty || 0), 0);
  return { sumItems, sumQty, sumTotal };
}
`)}
`,
validation33: `
<h2>3.3 Валідація форм</h2>

<p>
У веб-застосунку реалізовано комплексну систему валідації форм авторизації
та реєстрації користувачів, яка забезпечує коректність введених даних,
зручність взаємодії з інтерфейсом та базовий рівень безпеки.
Валідація виконується на клієнтській стороні за допомогою JavaScript
та доповнюється логікою імітації серверних перевірок.
</p>

<h3>Перевірка обов’язкових полів</h3>
<p>
Усі ключові поля форм є обов’язковими для заповнення. Для цього
використовуються стандартні HTML-атрибути
<code>required</code>, <code>type</code>, <code>minlength</code>,
а також додаткові перевірки у JavaScript.
</p>

<h4>Приклад перевірки обов’язкового текстового поля</h4>
${codeBlock(`
function validateRequiredText(input, labelName) {
  const v = input.value.trim();
  if (!v) return \`\${labelName} є обов’язковим(ою).\`;
  return "";
}
`)}

<p>
Дана функція використовується для полів імені та прізвища під час
реєстрації користувача.
</p>

<h3>Перевірка формату даних</h3>
<p>
Для полів з особливими вимогами до формату введення реалізовано
окремі валідатори.
</p>

<h4>Перевірка email-адреси за допомогою регулярного виразу</h4>
${codeBlock(`
const emailRegex = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/;

function validateEmail(input) {
  const v = input.value.trim();
  if (!v) return "Email є обов’язковим.";
  if (!emailRegex.test(v)) return "Некоректний формат email.";
  return "";
}
`)}

<h4>Перевірка пароля</h4>
<p>
Перевірка пароля включає контроль мінімальної довжини, що
унеможливлює використання занадто простих значень.
</p>

${codeBlock(`
function validatePassword(input) {
  const v = input.value;
  if (!v) return "Пароль є обов’язковим.";
  if (v.length < 6) return "Пароль має містити щонайменше 6 символів.";
  return "";
}
`)}

<h3>Валідація в реальному часі (live validation)</h3>
<p>
Для покращення користувацького досвіду реалізовано валідацію
в режимі реального часу. Перевірка виконується під час події
<code>input</code>, а у разі помилки поле підсвічується,
і користувачеві виводиться відповідне повідомлення.
</p>

<h4>Приклад live-валідації поля email</h4>
${codeBlock(`
email.addEventListener("input", () => 
  setFieldError(email, validateEmail(email))
);
`)}

<h4>CSS-оформлення некоректного поля</h4>
${codeBlock(`
.input.is-invalid {
  border-color: crimson;
  box-shadow: 0 0 0 3px rgba(220,20,60,.15);
}
`)}

<h3>Підсумкова валідація перед відправкою форми</h3>
<p>
Перед відправкою даних виконується повторна перевірка усіх полів
під час події <code>submit</code>. Якщо хоча б одне поле містить
помилку, форма не надсилається.
</p>

<h4>Приклад підсумкової валідації</h4>
${codeBlock(`
form.addEventListener("submit", (e) => {
  e.preventDefault();

  const eErr = validateEmail(email);
  const pErr = validatePassword(pass);

  setFieldError(email, eErr);
  setFieldError(pass, pErr);

  if (eErr || pErr) return;
});
`)}

<p>
Такий підхід гарантує цілісність даних навіть у разі вимкнення
live-валідації або ручної зміни DOM.
</p>

<h3>Обробка серверних помилок (імітація)</h3>
<p>
У межах лабораторної роботи реалізовано імітацію серверної
валідації через перевірку даних у <code>localStorage</code>.
Це дозволяє відобразити типові помилки, які виникають під час
взаємодії з реальним сервером.
</p>

<ul>
  <li>використання зайнятого email під час реєстрації;</li>
  <li>введення неправильного пароля;</li>
  <li>відсутність користувача з вказаним email.</li>
</ul>

<h4>Приклад обробки помилки зайнятого email</h4>
${codeBlock(`
if (users.some(u => u.email === emailVal)) {
  setFormMessage(form, "Цей email вже зайнятий. Спробуйте інший.");
  return;
}
`)}

<p>
Повідомлення про помилки виводяться у межах форми без перезавантаження
сторінки, що відповідає сучасним вимогам до UX та забезпечує
зручну взаємодію користувача із застосунком.
</p>
`,

api34: `
<h2>3.4 Робота з API (імітація REST-взаємодії на клієнтській стороні)</h2>

<p>
У веб-застосунку реалізовано логіку роботи з даними, аналогічну взаємодії
з REST API. Замість реального серверного бекенду та HTTP-запитів через
<code>fetch()</code> або <code>Axios</code> у проєкті використано локальні
джерела даних (<code>window.PRODUCTS</code>) та механізм
<code>LocalStorage</code>.
</p>

<p>
Такий підхід дозволяє повністю відпрацювати CRUD-операції, асинхронну
логіку та обробку помилок без залежності від зовнішнього сервера,
зберігаючи при цьому архітектуру, близьку до реальної клієнт-серверної
взаємодії.
</p>

<h3>GET-запити — отримання списків даних</h3>
<p>
Отримання списку товарів реалізовано через доступ до глобального масиву
<code>window.PRODUCTS</code>, який виконує роль відповіді API.
Подальша обробка включає фільтрацію, сортування та пагінацію даних.
</p>

<h4>Приклад «GET-запиту» (отримання товарів за категорією)</h4>
${codeBlock(`
function filterByFixedCategory(){
  const FIXED = window.FIXED_CATEGORY || null;
  const all = (window.PRODUCTS || []);
  return FIXED ? all.filter(p => p.category === FIXED) : all;
}
`)}

<p>
Отримані дані передаються у функції рендерингу для відображення карток
товарів у каталозі без перезавантаження сторінки.
</p>

<h3>POST-запити — надсилання форм (реєстрація, логін)</h3>
<p>
Надсилання форм авторизації та реєстрації реалізовано як створення нових
записів у <code>LocalStorage</code>, що імітує виконання POST-запиту до API.
</p>

<h4>Приклад «POST-запиту» реєстрації користувача</h4>
${codeBlock(`
const newUser = {
  id: "u-" + Date.now(),
  first_name: first.value.trim(),
  last_name: last.value.trim(),
  email: emailVal,
  passHash: hashPassword(pass.value),
  createdAt: Date.now(),
};

users.push(newUser);
saveUsers(users);
`)}

<p>
У разі успішної операції користувач автоматично авторизується, що
відповідає типовій серверній поведінці після успішного POST-запиту.
</p>

<h3>PUT / PATCH — оновлення даних</h3>
<p>
Операції оновлення реалізовані на прикладі роботи з кошиком.
При зміні кількості товару відбувається оновлення існуючого запису
у <code>LocalStorage</code>, що відповідає PATCH-запиту.
</p>

<h4>Приклад оновлення кількості товару</h4>
${codeBlock(`
function changeQty(id, delta) {
  const cart = loadCart();
  const idx = cart.items.findIndex(it => it.id === id);
  if (idx < 0) return;

  cart.items[idx].qty = clampQty((cart.items[idx].qty || 1) + delta);
  saveCart(cart);
}
`)}

<p>
Таким чином забезпечується коректне оновлення даних без перезавантаження
сторінки та з миттєвим оновленням інтерфейсу.
</p>

<h3>DELETE — видалення елементів</h3>
<p>
Видалення даних реалізовано шляхом фільтрації масиву та повторного
збереження стану кошика, що імітує виконання DELETE-запиту.
</p>

<h4>Приклад видалення товару з кошика</h4>
${codeBlock(`
function removeItem(id) {
  const cart = loadCart();
  cart.items = cart.items.filter(it => it.id !== id);
  saveCart(cart);
}
`)}

<p>
Такий підхід відповідає REST-принципам та дозволяє у майбутньому легко
замінити локальну логіку на реальні HTTP-запити до серверного API
без суттєвих змін у структурі застосунку.
</p>
`,
logic35: `
<h2>3.5 Опис бізнес-логіки</h2>

<p>
Бізнес-логіка веб-застосунку визначає правила роботи системи, порядок
обробки даних та поведінку інтерфейсу залежно від дій користувача.
У даному проєкті бізнес-логіка реалізована на клієнтській стороні
за допомогою JavaScript та базується на роботі з локальними даними
(<code>window.PRODUCTS</code>) і сховищем браузера
(<code>localStorage</code>).
</p>

<h3>Алгоритми роботи з даними</h3>
<p>
У застосунку реалізовано основні алгоритми обробки даних каталогу:
</p>
<ul>
  <li>пошук за назвою, описом та характеристиками товарів;</li>
  <li>сортування за ціною та рейтингом;</li>
  <li>фільтрація за категоріями;</li>
  <li>пагінація для поетапного відображення великого списку товарів.</li>
</ul>

<h4>Приклад логіки фільтрації та пошуку</h4>
${codeBlock(`
function applyFilters(list){
  const q = ($('#q')?.value || '').trim().toLowerCase();
  let items = list.slice();

  if (q) {
    items = items.filter(p =>
      p.title.toLowerCase().includes(q) ||
      p.desc.toLowerCase().includes(q) ||
      Object.values(p.specs || {}).some(v =>
        String(v).toLowerCase().includes(q)
      )
    );
  }

  return items;
}
`)}

<h4>Приклад реалізації пагінації</h4>
${codeBlock(`
const start = (currentPage - 1) * PER_PAGE;
const pageItems = filtered.slice(start, start + PER_PAGE);
`)}

<h3>Умовна логіка інтерфейсу</h3>
<p>
Залежно від стану даних та дій користувача інтерфейс змінює свою поведінку.
Ключовим прикладом є робота з кошиком.
</p>

<h4>Приклад: порожній кошик</h4>
<ul>
  <li>таблиця з товарами приховується;</li>
  <li>кнопка <b>«Оформити»</b> стає неактивною.</li>
</ul>

${codeBlock(`
if (items.length === 0) {
  empty.hidden = false;
  table.hidden = true;
  btnCheckout.disabled = true;
}
`)}

<h4>Приклад: заповнений кошик</h4>
${codeBlock(`
btnCheckout.disabled = false;
table.hidden = false;
`)}

<h3>Правила взаємодії користувача з UI</h3>
<p>
Бізнес-логіка визначає чіткі правила взаємодії з елементами інтерфейсу:
</p>
<ul>
  <li>некоректно заповнена форма → показуються повідомлення про помилки;</li>
  <li>успішна дія → показується toast або виконується редирект;</li>
  <li>логічна помилка → повідомлення без перезавантаження сторінки.</li>
</ul>

<h4>Приклад логіки валідації форми</h4>
${codeBlock(`
if (eErr || pErr) return;
`)}

<h4>Приклад повідомлення про помилку</h4>
${codeBlock(`
setFormMessage(form, "Невірний пароль. Спробуйте ще раз.");
`)}

<h4>Приклад повідомлення про успішну дію (додавання в кошик)</h4>
${codeBlock(`
toast(\`Додано в кошик: \${prod.title}\`);
`)}

<h3>Робота з ролями та рівнями доступу</h3>
<p>
У межах проєкту реалізовано базову модель доступу:
</p>
<ul>
  <li><b>Гість</b> — може переглядати каталог, відкривати деталі товарів та додавати їх у кошик;</li>
  <li><b>Авторизований користувач</b> — має сесію, що дозволяє ідентифікувати його стан входу.</li>
</ul>

<h4>Приклад збереження сесії користувача</h4>
${codeBlock(`
localStorage.setItem("pc_session", JSON.stringify({
  email: user.email,
  loggedInAt: Date.now()
}));
`)}

<p>
Розмежування ролей може бути розширене у майбутньому
(адміністратор, менеджер тощо).
</p>

<h3>Обробка складних сценаріїв</h3>

<h4>Створення замовлення (демо-сценарій)</h4>
<p>
Алгоритм оформлення замовлення включає:
</p>
<ul>
  <li>перевірку, чи кошик не порожній;</li>
  <li>виведення повідомлення про успішне оформлення;</li>
  <li>очищення кошика.</li>
</ul>

${codeBlock(`
if (!cart.items.length) return;
alert("Дякуємо! (демо) Замовлення сформовано.");
clearCart();
`)}

<h4>Оновлення стану кошика</h4>
<p>
Бізнес-логіка кошика забезпечує:
</p>
<ul>
  <li>зміну кількості товарів;</li>
  <li>автоматичний перерахунок суми;</li>
  <li>синхронізацію стану між вкладками браузера.</li>
</ul>

<p>
Таким чином бізнес-логіка забезпечує цілісну, передбачувану та зручну
роботу веб-застосунку відповідно до вимог онлайн-каталогу
комп’ютерних комплектуючих.
</p>
`,

section34: `
<h2>3 РОЗДІЛ. РЕАЛІЗАЦІЯ КЛІЄНТСЬКОГО ЗАСТОСУНКУ (JavaScript)</h2>

<h3>3.1 Обробка подій DOM (DOM-event handling)</h3>
<p>
У застосунку реалізовано механізм <b>DOM-event handling</b>, що забезпечує
реакцію інтерфейсу на дії користувача: кліки, введення даних, відправку форм,
зміну розміру вікна. Обробка подій виконується за допомогою
<code>addEventListener</code> та делегування подій.
</p>

<h4>3.1.1 Бургер-меню та адаптивна навігація</h4>
<p>
Адаптивна навігація реалізована у вигляді бургер-меню, яке змінює свій стан
залежно від дій користувача та ширини екрану. При кліку змінюється атрибут
<code>data-open</code>, який використовується у CSS для показу або приховування меню.
</p>

${codeBlock(`
document.addEventListener('DOMContentLoaded', () => {
  const burger = document.querySelector('.burger');
  const nav = document.getElementById('nav');

  burger.addEventListener('click', () => {
    const isOpen = nav.getAttribute('data-open') === 'true';
    nav.setAttribute('data-open', String(!isOpen));
    burger.setAttribute('aria-expanded', String(!isOpen));
  });

  function autoToggleMenu() {
    if (window.innerWidth <= 768) {
      nav.setAttribute('data-open', 'true');
      burger.setAttribute('aria-expanded', 'true');
    } else {
      nav.setAttribute('data-open', 'false');
      burger.setAttribute('aria-expanded', 'false');
    }
  }

  autoToggleMenu();
  window.addEventListener('resize', autoToggleMenu);
});
`)}

<h4>3.1.2 Перехід по категоріях каталогу</h4>
<p>
Для навігації між категоріями використано перехоплення події
<code>click</code> на посиланнях карток. Залежно від параметра
<code>category</code> у URL відбувається перенаправлення на відповідну сторінку.
</p>

${codeBlock(`
const map = {
  cpu:'cpu.html', mb:'mb.html', ram:'ram.html', gpu:'gpu.html',
  storage:'storage.html', case:'case.html', psu:'psu.html', cooling:'cooling.html'
};

document.querySelectorAll('.catalog .card__link').forEach(a=>{
  a.addEventListener('click', (e)=>{
    const href = a.getAttribute('href')||'';
    let cat = null;
    try { cat = new URL(href, location.href).searchParams.get('category'); } catch(e){}
    if (cat && map[cat]) { 
      e.preventDefault(); 
      location.href = map[cat]; 
    }
  });
});
`)}

<h3>3.2 Робота з локальним сховищем (localStorage)</h3>
<p>
Для збереження стану застосунку використовується <code>localStorage</code>.
У ньому зберігаються кошик користувача (<code>pc_cart</code>), список користувачів
(<code>pc_users</code>) та активна сесія (<code>pc_session</code>).
</p>

<h4>3.2.1 Лічильник товарів у кошику</h4>
<p>
Лічильник біля іконки кошика відображає загальну кількість товарів.
Для синхронізації між вкладками використовується подія <code>storage</code>.
</p>

${codeBlock(`
(function(){
  const KEY = 'pc_cart';
  function count(){
    const c = JSON.parse(localStorage.getItem(KEY)) || {items:[]};
    return c.items.reduce((s,i)=>s+i.qty,0);
  }
  function update(){
    var el = document.getElementById('cartCount');
    if(el) el.textContent = String(count());
  }
  window.addEventListener('storage', update);
  document.addEventListener('DOMContentLoaded', update);
})();
`)}

<h3>3.3 Каталог товарів: фільтрація, сортування, пагінація, модальне вікно</h3>
<p>
Дані каталогу зберігаються у масиві <code>window.PRODUCTS</code>.
Реалізовано пошук, сортування, пагінацію та перегляд детальної інформації.
</p>

<h4>3.3.1 Фільтрація та пошук</h4>

${codeBlock(`
function applyFilters(list){
  const q = ($('#q')?.value||'').trim().toLowerCase();
  const sort = $('#sort')?.value;
  let items = list.slice();

  if(q) {
    items = items.filter(p =>
      p.title.toLowerCase().includes(q) ||
      p.desc.toLowerCase().includes(q) ||
      Object.values(p.specs||{}).some(v => 
        String(v).toLowerCase().includes(q)
      )
    );
  }

  if (sort === 'price_asc') items.sort((a,b)=>a.price-b.price);
  else if (sort === 'price_desc') items.sort((a,b)=>b.price-a.price);
  else if (sort === 'rating_desc') items.sort((a,b)=>b.rating-a.rating);

  return items;
}
`)}

<h4>3.3.2 Пагінація</h4>

${codeBlock(`
const PER_PAGE = 6;
let currentPage = 1;

function render(){
  const filtered = applyFilters(filterByFixedCategory());
  const totalPages = Math.max(1, Math.ceil(filtered.length / PER_PAGE));
  if (currentPage > totalPages) currentPage = totalPages;

  const start = (currentPage - 1) * PER_PAGE;
  const pageItems = filtered.slice(start, start + PER_PAGE);

  renderGrid(pageItems);
  renderPagination(filtered.length);
}
`)}

<h4>3.3.3 Модальне вікно з деталями товару</h4>

${codeBlock(`
function openModal(id){
  const p = (window.PRODUCTS||[]).find(x=>x.id===id);
  if(!p) return;

  $('#m-img').src = p.img;
  $('#m-title').textContent = p.title;
  $('#m-sku').textContent = \`SKU: \${p.sku}\`;
  $('#m-desc').textContent = p.desc;

  const specs = $('#m-specs');
  specs.innerHTML = '';
  Object.entries(p.specs||{}).forEach(([k,v])=>{
    const d = document.createElement('div');
    d.innerHTML = \`<div class="muted">\${k}</div><div style="font-weight:600">\${v}</div>\`;
    specs.appendChild(d);
  });

  $('#modal').showModal();
}
`)}

<h3>3.4 Реалізація кошика</h3>

<h4>3.4.1 Додавання товару</h4>

${codeBlock(`
function addToCart(prod, qty=1){
  const cart = loadCart();
  const idx = cart.items.findIndex(i => i.id === prod.id);

  if(idx >= 0) cart.items[idx].qty += qty;
  else cart.items.push({ 
    id: prod.id, title: prod.title, 
    price: prod.price, img: prod.img, qty 
  });

  saveCart(cart);
  toast(\`Додано в кошик: \${prod.title}\`);
}
`)}

<h4>3.4.2 Делегування подій у кошику</h4>

${codeBlock(`
body.addEventListener("click", (e) => {
  const btn = e.target.closest("button[data-action]");
  if (!btn) return;

  const action = btn.dataset.action;

  if (action === "remove") {
    const id = btn.dataset.id;
    if (id) removeItem(id);
    return;
  }

  const wrap = btn.closest(".qty");
  const id = wrap?.dataset.id;
  if (!id) return;

  if (action === "inc") changeQty(id, +1);
  if (action === "dec") changeQty(id, -1);
});
`)}

<h4>3.4.3 Бізнес-правило: порожній кошик</h4>

${codeBlock(`
if (items.length === 0) {
  empty.hidden = false;
  table.hidden = true;
  btnCheckout.disabled = true;
} else {
  empty.hidden = true;
  table.hidden = false;
  btnCheckout.disabled = false;
}
`)}
`,
section3_5: `
<h3>3.5 Додатковий інтерактивний модуль: калькулятор</h3>

<p>
У межах проєкту реалізовано <b>додатковий інтерактивний модуль — калькулятор</b>,
який працює у модальному вікні та не потребує перезавантаження сторінки.
Даний модуль демонструє використання JavaScript для побудови окремої
інтерактивної підсистеми з власною логікою обробки подій та обчислень.
</p>

<p>
Калькулятор підтримує базові арифметичні операції
(<b>додавання, віднімання, множення, ділення</b>),
а також розширені математичні дії:
<b>відсотки, піднесення до степеня та обчислення квадратного кореня</b>.
</p>

<p>
Використання модального калькулятора є прикладом розширення функціональності
веб-застосунку без впливу на основну бізнес-логіку каталогу та кошика.
</p>

<h4>3.5.1 Відкриття та закриття модального вікна калькулятора</h4>
<p>
Модальне вікно калькулятора відкривається при натисканні на відповідну іконку
та закривається при кліку на кнопку закриття або на фон модального вікна.
Уся логіка реалізована через обробку подій <code>click</code>.
</p>

${codeBlock(`
icon.addEventListener("click", () => {
  modal.classList.remove("hidden");
});

closeBtn.addEventListener("click", () => {
  modal.classList.add("hidden");
});

modal.addEventListener("click", (e) => {
  if (e.target === modal) modal.classList.add("hidden");
});
`)}

<p>
Такий підхід дозволяє забезпечити зручну взаємодію з інтерфейсом та відповідає
сучасним UX-вимогам до модальних компонентів.
</p>

<h4>3.5.2 Обчислення математичних виразів</h4>
<p>
Основна логіка калькулятора реалізована у вигляді окремої функції
<code>calculate</code>, яка приймає два числових аргументи та оператор.
Залежно від переданого оператора виконується відповідна математична операція.
</p>

${codeBlock(`
function calculate(a, b, op) {
  switch (op) {
    case "+": return a + b;
    case "-": return a - b;
    case "*": return a * b;
    case "/": return b === 0 ? "Error" : a / b;
    case "%": return a % b;
    case "^": return Math.pow(a, b);
    default: return b;
  }
}
`)}

<p>
Функція містить базову перевірку помилок, зокрема ділення на нуль,
та може бути легко розширена новими операціями.
Реалізація калькулятора демонструє модульний підхід до побудови
JavaScript-логіки та можливість інтеграції додаткових інструментів
у межах одного веб-застосунку.
</p>
`,

};

  function setActiveTab(id) {
    tabs.forEach(t => t.classList.toggle('active', t.dataset.lab === String(id)));
  }
  function setActivePill(menuEl, sec) {
    const btns = Array.from(menuEl.querySelectorAll('.pill'));
    btns.forEach(b => b.classList.toggle('active', b.dataset.section === sec));
  }

  function renderLab(labId, section) {
    setActiveTab(labId);

    sideMenuLab1.style.display = (labId === 1) ? 'block' : 'none';
    sideMenuLab2.style.display = (labId === 2) ? 'block' : 'none';
    sideMenuLab3.style.display = (labId === 3) ? 'block' : 'none';
    sideMenuLab4.style.display = (labId === 4) ? 'block' : 'none';
    sideMenuLab5.style.display = (labId === 5) ? 'block' : 'none';
    sideMenuLab6.style.display = (labId === 6) ? 'block' : 'none';
    sideMenuLab7.style.display = (labId === 7) ? 'block' : 'none';
    sideMenuLab8.style.display = (labId === 8) ? 'block' : 'none';
    

    if (![1, 2, 3, 4, 5, 6, 7, 8].includes(labId)) {
      content.innerHTML = `<h2>Лабораторна робота №${labId}</h2><p>Матеріали будуть додані пізніше.</p>`;
      return;
    }

    const secDefault =
    labId === 1 ? 'theme' :
    labId === 2 ? 'sel-tags' :
    labId === 3 ? 'theme3' :
    labId === 4 ? 'theme4' :
    labId === 5 ? 'theme5' :
    labId === 6 ? 'theme6' :
    labId === 7 ? 'theme7' :
    labId === 8 ? 'theme8' :
    null;


    const sec = section || secDefault;


    if (labId === 1) {
      setActivePill(sideMenuLab1, sec);
      content.innerHTML = lab1[sec] || `<p>Розділ “${sec}” у підготовці.</p>`;
    } else if (labId === 2) {
      setActivePill(sideMenuLab2, sec);
      content.innerHTML = lab2[sec] || `<p>Розділ “${sec}” у підготовці.</p>`;
    } else if (labId === 3) {
      setActivePill(sideMenuLab3, sec);
      content.innerHTML = lab3[sec] || `<p>Розділ “${sec}” у підготовці.</p>`;
    } else if (labId === 4) {
      setActivePill(sideMenuLab4, sec);
      content.innerHTML = lab4[sec] || `<p>Розділ “${sec}” у підготовці.</p>`;
    } else if (labId === 5) {
      setActivePill(sideMenuLab5, sec);
      content.innerHTML = lab5[sec] || `<p>Розділ “${sec}” у підготовці.</p>`;
    } else if (labId === 6) {
      setActivePill(sideMenuLab6, sec);
      content.innerHTML = lab6[sec] || `<p>Розділ “${sec}” у підготовці.</p>`;
    } else if (labId === 7) {
      setActivePill(sideMenuLab7, sec);
      content.innerHTML = lab7[sec] || `<p>Розділ “${sec}” у підготовці.</p>`;
    } else if (labId === 8) {
      setActivePill(sideMenuLab8, sec);
      content.innerHTML = lab8[sec] || `<p>Розділ “${sec}” у підготовці.</p>`;
    }


  }

  function navigate(labId, sec, push = true) {
    renderLab(labId, sec);
    if (push) {
      const params = new URLSearchParams();
      params.set('lab', labId);
      if (sec) params.set('sec', sec);
      history.pushState({ lab: labId, sec }, '', `?${params.toString()}`);
      document.title = `ЛР №${labId} — Звіти`;
    }
  }

  
  tabs.forEach(btn => {
    btn.addEventListener('click', () => {
      const id = Number(btn.dataset.lab);
      const firstSec =
      id === 1 ? 'theme' :
      id === 2 ? 'sel-tags' :
      id === 3 ? 'theme3' :
      id === 4 ? 'theme4' :
      id === 5 ? 'theme5' :
      id === 6 ? 'theme6' :
      id === 7 ? 'theme7' :
      id === 8 ? 'theme8' :  
      null;
      navigate(id, firstSec, true);
    });
  });


  
  sideMenuLab1.addEventListener('click', (e) => {
    const btn = e.target.closest('.pill'); if (!btn) return;
    navigate(1, btn.dataset.section, true);
  });

  sideMenuLab2.addEventListener('click', (e) => {
    const btn = e.target.closest('.pill'); if (!btn) return;
    navigate(2, btn.dataset.section, true);
  });

  sideMenuLab3.addEventListener('click', (e) => {
  const btn = e.target.closest('button'); 
  if (!btn) return;

  if (btn.classList.contains('pill-parent')) {
    const key = btn.dataset.toggle;
    const sub = sideMenuLab3.querySelector(`.sub-menu[data-parent="${key}"]`);
    if (sub) sub.classList.toggle('open');
    return;
  }

  if (btn.classList.contains('pill') && btn.dataset.section) {
    navigate(3, btn.dataset.section, true);
  }
  });

  sideMenuLab4.addEventListener('click', (e) => {
    const btn = e.target.closest('.pill'); 
    if (!btn) return;
    navigate(4, btn.dataset.section, true);
  });

sideMenuLab5.addEventListener("click", (e) => {
  const btn = e.target.closest("button");
  if (!btn) return;

  if (btn.classList.contains("pill-parent")) {
    const key = btn.dataset.toggle;
    const sub = sideMenuLab5.querySelector(`.sub-menu[data-parent="${key}"]`);
    if (sub) sub.classList.toggle("open");
    return;
  }

  if (btn.dataset.section) {
    navigate(5, btn.dataset.section, true);
  }
});

sideMenuLab6.addEventListener("click", (e) => {
  const btn = e.target.closest("button");
  if (!btn) return;

  if (btn.classList.contains("pill-parent")) {
    const key = btn.dataset.toggle;
    const sub = sideMenuLab6.querySelector(`.sub-menu[data-parent="${key}"]`);
    if (sub) sub.classList.toggle("open");
    return;
  }

  if (btn.dataset.section) {
    navigate(6, btn.dataset.section, true);
  }
});

sideMenuLab7.addEventListener("click", (e) => {
  const btn = e.target.closest("button");
  if (!btn) return;

  if (btn.classList.contains("pill-parent")) {
    const key = btn.dataset.toggle;
    const sub = sideMenuLab7.querySelector(`.sub-menu[data-parent="${key}"]`);
    if (sub) sub.classList.toggle("open");
    return;
  }

  if (btn.dataset.section) {
    navigate(7, btn.dataset.section, true);
  }
});

sideMenuLab8.addEventListener("click", (e) => {
  const btn = e.target.closest("button");
  if (!btn) return;

  if (btn.classList.contains("pill-parent")) {
    const key = btn.dataset.toggle;
    const sub = sideMenuLab8.querySelector(`.sub-menu[data-parent="${key}"]`);
    if (sub) sub.classList.toggle("open");
    return;
  }

  if (btn.dataset.section) {
    navigate(8, btn.dataset.section, true);
  }
});



  window.addEventListener('popstate', () => {
    const params = new URLSearchParams(location.search);
    const lab = Number(params.get('lab')) || 1;
    const sec = params.get('sec') ||
      (lab === 1 ? 'theme' :
      lab === 2 ? 'sel-tags' :
      lab === 3 ? 'theme3' :
      lab === 4 ? 'theme4' :
      lab === 5 ? 'theme5' :
      lab === 6 ? 'theme6' :
      lab === 7 ? 'theme7' :
      lab === 8 ? 'theme8' :
  null);
    renderLab(lab, sec);
  });
  (function boot() {
    const params = new URLSearchParams(location.search);
    const lab = Number(params.get('lab')) || 1;
    const sec = params.get('sec') ||
      (lab === 1 ? 'theme' :
      lab === 2 ? 'sel-tags' :
      lab === 3 ? 'theme3' :
      lab === 4 ? 'theme4' :
      lab === 5 ? 'theme5' :
      lab === 6 ? 'theme6' :
      lab === 7 ? 'theme7' :
      lab === 8 ? 'theme8' :
      null);
    renderLab(lab, sec);
  })();
})();




document.addEventListener('click', (event) => {
  // Завдання 1 — SHOW ME
  if (event.target.id === 'showMeBtn') {
    const input = document.getElementById('showMeInput');
    if (!input) return;
    console.log('Введене значення:', input.value);
  }

  // Завдання 2 — Приховати / Розкрити
  if (event.target.id === 'passwordToggleBtn') {
    const input = document.getElementById('passwordInput');
    if (!input) return;

    if (input.type === 'text') {
      // Приховуємо текст
      input.type = 'password';
      event.target.textContent = 'Розкрити';
    } else {
      // Розкриваємо текст
      input.type = 'text';
      event.target.textContent = 'Приховати';
    }
  }
});

// Завдання 5 — Визначення кліку всередині блоку з id="place"
window.addEventListener('click', function (event) {
  const place = document.getElementById('place');
  if (!place) return;

  const clickedInside = place.contains(event.target);
  console.log(clickedInside);
});


document.addEventListener('submit', (event) => {
  // Перевіряємо, що сабміт саме форми логіна
  const form = event.target;
  if (!form.classList.contains('login-form')) return;

  // 2. Забороняємо перезавантаження сторінки
  event.preventDefault();

  // 4. Дістаємо елементи форми через form.elements
  const { email, password } = form.elements;

  const emailValue = email.value.trim();
  const passwordValue = password.value.trim();

  // 3. Перевірка заповненості полів
  if (!emailValue || !passwordValue) {
    alert('All form fields must be filled in');
    return;
  }

  // 4. Формуємо об'єкт з даними
  const formData = {
    email: emailValue,
    password: passwordValue,
  };

  // 5. Виводимо в консоль і очищуємо форму
  console.log(formData);
  form.reset();
});


// Завдання 6 Функція для генерації випадкового hex-кольору
function getRandomHexColor() {
  return `#${Math.floor(Math.random() * 16777215)
    .toString(16)
    .padStart(6, 0)}`;
}

// зміна кольору фону
document.addEventListener('click', (event) => {
  // Перевіряємо клік по кнопці .change-color
  if (event.target.classList.contains('change-color')) {
    const newColor = getRandomHexColor();

    // Змінюємо фон тіла документа
    document.body.style.backgroundColor = newColor;

    // Виводимо колір у span.color
    const colorSpan = document.querySelector('.color');
    if (colorSpan) {
      colorSpan.textContent = newColor;
    }
  }
});


function getRandomHexColor() {
  return `#${Math.floor(Math.random() * 16777215)
    .toString(16)
    .padStart(6, 0)}`;
}

function createBoxes(amount) {
  const boxesContainer = document.getElementById('boxes');
  if (!boxesContainer) return;

  boxesContainer.innerHTML = '';
  const boxes = [];
  let size = 30;

  for (let i = 0; i < amount; i++) {
    const box = document.createElement('div');
    box.style.width = `${size}px`;
    box.style.height = `${size}px`;
    box.style.backgroundColor = getRandomHexColor();
    boxes.push(box);
    size += 10;
  }

  boxesContainer.append(...boxes);
}

function destroyBoxes() {
  const boxesContainer = document.getElementById('boxes');
  if (!boxesContainer) return;
  boxesContainer.innerHTML = '';
}

document.addEventListener('click', (event) => {
  if (event.target.matches('#controls [data-create]')) {
    const controls = document.getElementById('controls');
    const input = controls.querySelector('input[type="number"]');
    const value = Number(input.value.trim());

    if (value < 1 || value > 100) {
      alert('Value must be between 1 and 100');
      return;
    }

    createBoxes(value);
    input.value = '';
  }

  if (event.target.matches('#controls [data-destroy]')) {
    destroyBoxes();
  }
});
